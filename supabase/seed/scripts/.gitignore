# Generated by mine_recipes.ts — regenerable, not committed. The curated
# outputs (seed.sql, evals cases) are what get committed, not these raw runs.
out/

# The FAO density PDF and the throwaway venv extract_fao_density.py needs.
# Like the USDA CSV bundles, the raw source stays out — only the derived
# ../fao_density.jsonl is committed (see fao_density.md).
density_DB_v2_0*.pdf
.venv/
__pycache__/
