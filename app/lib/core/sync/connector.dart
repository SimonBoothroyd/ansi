/// PowerSync <-> Supabase backend connector: uploads the local write queue and
/// provides credentials.
///
// TODO(step-7): implement PowerSyncBackendConnector (fetchCredentials +
/// uploadData) against Supabase Auth. See docs/references (PowerSync) once fetched.
library;
