/// The app's PowerSync database, exposed as a Riverpod provider so repositories
/// can watch reactive queries.
///
// TODO(step-7): open the PowerSyncDatabase(schema: schema), connect it with the
/// backend connector, and expose it (and query-stream providers) here.
library;
