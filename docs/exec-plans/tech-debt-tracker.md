# Tech debt tracker

Known, deliberate debt. Pay it down in small, continuous increments rather than
letting it compound. Add an item when you knowingly cut a corner; remove it when
you fix it.

| Added | Area | Debt | Cost if ignored | Plan |
|-------|------|------|-----------------|------|
| 2026-08-24 | scaffold | Version floors in `app/pubspec.yaml` are caret ranges. `pubspec.lock` **is** committed now, so resolution is pinned in practice. | Loosened floors could drift on a `pub upgrade`. | Tighten floors when a resolve conflict actually shows; the lock carries it until then. |
| 2026-08-26 | recipes/ui | Two Forui 0.22.3 workarounds: a custom underline tab bar (Forui's `FTabsStyle` underline needs the un-exported `FVariants`) and a custom hairline (`FDivider` bakes in a 16px margin). | Diverges from Forui; extra code to maintain. | Revisit if Forui exposes an underline `FTabsStyle` label style and zero-margin dividers. |
| 2026-08-26 | books/ui | Section reorder is up/down menu items, not drag-and-drop. | Clunky for many sections. | Add a reorderable list when Forui offers a drag handle, or wrap Material's `ReorderableListView`. |
| 2026-08-26 | books | Provider-lifecycle bugs (the `LibraryActions` crash: `ref` used after an async dialog) are covered by exactly one path — the `make test-sim` smoke, which is local-only. Every other async mutation path is unguarded. | A lifecycle bug on an uncovered path still reaches the phone. | Widen the smoke, or add widget tests over the real-DB harness for each mutation flow. Standing rule meanwhile: mutations go through keep-alive repo providers. |
| 2026-08-27 | app/test | Host repo tests need an extension-capable SQLite; macOS's system build omits extension loading, so `test_db.dart` falls back to Homebrew's `libsqlite3.dylib`. The Linux/CI path (`libsqlite3-dev` + the fetched extension) has not run yet. | A machine without `brew install sqlite` can't run `make test-app`; CI may need a tweak on its first run. | Confirm on the next CI run; revisit if the sqlite3 package ever ships a bundled host library. |
| 2026-08-27 | app/ui | Opening a Forui dialog with the semantics tree live trips a framework assertion (`node.isMergedIntoParent`), so `integration_test/app_test.dart` filters that one assertion. Reproduces in a plain widget test with `ensureSemantics()`. | Real assistive-tech users may hit a malformed semantics tree; the filter could mask a future regression. | Re-check when forui moves past 0.22.x; drop the filter if it's fixed, otherwise report upstream. |
| 2026-08-28 | cook_plan/shopping | **Nudge/shop-list scale gap** (accepted 7.6 edge): the whole-batch nudge advises cooking ×1 while the shopping list keeps buying the honest raw factor (×0.75) — the toggle is display-only by decision (plan 0010). The nudge line now says "shopping still buys ×0.75" so the gap is visible. | A cook who follows the advice under-buys and must top up by eye. | Persist the nudge choice and let the shop list read it (the same persisted per-session override store as the deferred cook-day adjustment) — candidate for 7.7 or step 11. |
| 2026-08-28 | cook_plan/ui | The **whole-batch toggle** on a session tile is ephemeral widget-scoped state (an autoDispose provider): it resets when the tile scrolls out of the ListView or the app restarts. Persisting it was explicitly ruled out of 7.6 (plan 0010 decision log). | A cook re-toggles after scrolling; two devices don't share the view choice. | Revisit only if the reset annoys in practice — a persisted per-session override store is the same machinery the deferred cook-day adjustment needs. |
| 2026-08-27 | cook_plan | **Cook-day adjustment** (step-5 title) is display-only — the timeline handle isn't draggable; cook day is always the earliest covered day. | Can't move a batch's cook day off the default. | Add a persisted per-session override store (the plan is otherwise purely derived) when there's demand; stretch. |
| 2026-08-27 | cook_plan/ui | The freshness timeline (`_TrackPainter`) draws on a fixed Mon→Sun axis with a 7-day ruler. `CookTimelineSpec` is unit-tested (day coords + tails); the painted bar is sim-verified (fresh/split + freezer/blue) but not golden-tested, and multiple meals on one day collapse to a single marker. | A rendering regression could slip; a two-meals-one-day session under-marks. | Add a golden test if the bar changes often; badge multi-meal days if it matters. |
| 2026-08-27 | ingredients | **FAO/INFOODS density fallback** (spec §6) still not ingested — but the famine is over: 7.8's portion-text density derivation (plan 0013 — SR Legacy keys volume portions by modifier, which the old parser missed) lifted vocab coverage **7/291 → 211/291**; the 2026-08-29 audit round (curation density corrections/fills + R1: volume-default ⇒ density, asserted at seed time) brings it to **264/307**. The fallback now only covers the ~43-row tail (count-default stubs + foods without FDC volume portions). | The tail's volume↔mass conversions still split into honest subtotals; those recipes stay `incomplete`. | Ingest FAO/INFOODS Density DB v2.0 as a prefill fallback for the remaining density-less rows — lower priority now. |
| 2026-08-27 | recipes/ui | **Cook mode + inline method chips/timers** and the **Notes tab** from the step-2 design board are unbuilt. Deferred from step 2; no target step. | Recipe page diverges from the design board; no hands-free cooking view. | Build as a follow-up feature slice when prioritised; re-check the design board first. |
| 2026-08-27 | recipes | **Recipe photos** are unbuilt. Deferred from step 2 because they needed Storage + auth — step 7 shipped auth, so this is now **unblocked**. | The recipe hero stays photo-less; the design board shows photos. | Supabase Storage bucket + upload/display slice (photos in Storage, not the DB — spec §2). |
| 2026-08-27 | books/ui | **Per-book detail screen** is unbuilt — all books render inline on the Library screen. Deferred from step 3. | Library screen gets long as books grow; no per-book landing page. | Add a book detail route when book count makes the inline list unwieldy. |
| 2026-08-27 | shopping | **"Clear list / new trip"** is unbuilt. Deferred from step 6 to step 7 as a sync concern, but step 7 did not ship it. | Check-state and manual top-ups accumulate forever; no way to start a fresh trip. | Add a clear/new-trip action (reset check-state + soft-delete manual contributions), synced like any other overlay write. |
| 2026-08-27 | sync/test | Sync remains untested for: **offline-queue drain on network loss** (airplane-mode → reconnect) and a **true two-client concurrent-edit session** (LWW / independent-row union, spec §3). `uploadData`'s put/patch/soft-delete mapping and fatal/transient handling are now unit-tested, and duplicate shopping-entry merge is repo-tested — those parts of this row are paid. | A regression in reconnect or cross-device conflict handling would slip. | An airplane-mode → reconnect test; a two-client sync scenario (possibly two sim clones against the local stack). |
| 2026-08-27 | auth/dev | **Ghost users after `supabase db reset`**: a device holding a still-valid JWT for a reset-deleted user drives the app as a user with no DB rows — `ensure_onboarded` hits FK violations until the token expires (≤1 h). Found while building the auth-aware smoke (it sidesteps via in-memory auth storage). | Confusing dev-loop failures after every reset. | Detect the 4xx from `ensure_onboarded` in the session controller's error state and offer sign-out (the retry/sign-out affordance already exists), or document "sign out after reset". |
| 2026-08-28 | auth | Google OAuth is now verified end-to-end against cloud, but the fix for the non-dismissing in-app browser sheet (`authScreenLaunchMode: LaunchMode.externalApplication` in `sign_in_view.dart`) was applied *after* that run and has not itself been exercised. | If the external-browser flow misbehaves (e.g. Safari foreground juggling), the sign-in UX regresses on the primary auth path. | One more cloud Google sign-in on the sim; confirm the app foregrounds cleanly, then delete this row. |
| 2026-08-28 | ingredients | Picker add-new stubs write `match_text` with the app's character-level normalizer only — the server's full phrase rules (singularize, word classes) are a step-8 artifact, so a stub's match_text can differ from what the server would write for the same name. | Import matching (step 8) may miss a locally created stub until its match_text is re-normalized server-side. | Step 8's normalizer re-writes match_text on flesh-out/import; revisit when the shared normalizer is callable from the app path. |
| 2026-08-28 | pickers/ui | The ingredient picker's "Recent" section derives from live usage (recipe lines + manual top-ups), not a selection history — picking something and then deleting its line drops it from Recent, and a fresh household shows the alphabetical list instead. | Recents are slightly less sticky than a true history. | Add a local selection-history store only if the derived feed annoys in practice. |
| 2026-08-29 | seed/usda | `seed_usda.sql` regenerated on **Foundation 2025-04-24** (the previous file came from a different Foundation release: 8262 → 8204 foods, macros refreshed for revised rows). All 248 `usda_links.jsonl` links resolve in the new bundle; unlinked dropped foods only shrink the create-search reference. | A future regeneration from yet another release churns the file again; linked macros can shift slightly between releases. | Record the bundle version in the generated header next time the generator is touched; re-verify links on every regeneration (the seed's trailing check already fails loudly). |
| 2026-08-29 | ingredients/ui | The 7.8 **density entry** (g/ml ⇄ "a spoon weighs…", volume-label redirect, live chip unlock) is covered by widget/repo/unit tests but has **no `make test-sim` scenario** — the smoke exercises the measure add path only. | A regression in the density flow's real-stack wiring (RLS on the ingredient update, sync round-trip of `allowed_units`) would surface only on-device. | Fold a density-entry step into the smoke's library scenario next time it's touched. |
| 2026-08-29 | ingredients | A density arriving **server-side** (template reseed, cloud operator) updates `density_g_per_ml` but not an already-materialized household `allowed_units` list — only the in-app `setDensity` path extends the list in the same write. | A reseeded density leaves the other family locked until the flesh-out form (step 8) edits the list. | Step 8's flesh-out form is the editing surface; if operator reseeds become routine before then, add an `allowed_units` refresh leg to the rollout snippet in `docs/cloud-setup.md`. |
| 2026-08-28 | recipes | The `Recipe` aggregate doesn't carry `favorite`; the recipe page resolves the flag from the list-summary stream instead (an extra watch, and `saveRecipe` can't clobber the flag — by design). | Mild indirection; a second subscription on the recipe page. | Fold `favorite` into the aggregate if the page grows more list-summary needs. |
| 2026-08-31 | import/library | **Library doesn't refresh after an import commit** — a freshly-imported recipe doesn't appear until you switch tabs and back (forces a re-watch). The library's watched query isn't re-firing on the import commit's writes. | A user imports a recipe and thinks it failed — it's saved, just not shown. | Likely the LEFT-JOIN watch trap ([[mise-powersync-watch-left-join]]): select a column from every table the import commit touches (recipe + groups + line-items + measures) so SQLite keeps them in the watch set. |
| 2026-08-31 | import/ui | **Reconciliation/recipe-view polish** (collected in `active/import-v1-jank.md`): method-step chips render the amount but drop the ingredient label ("Add the chopped 1, 0.5…"); the amount editor lists an ingredient's whole measure set (~13 chips for Tomato); no-quantity/imprecise items fall back to the raw line in the amount slot; the "from source" line dups the amount ("2–3 cloves garlic cloves"); the header "N to review" doesn't decrement as lines resolve. | Rough first-cut UX; the amount-only method chips read as broken. | Polish pass off the jank log — fold the ref label into the chip fold, prioritise/collapse unit chips, blank-not-raw amounts, de-dupe the source line, reconcile the two counters. |
| 2026-08-31 | import | **Photo path — real-device camera capture untested.** The gallery path is now sim-verified end-to-end (pick → crop/rotate → client downscale → Claude vision → reconcile, on a Dense Bean Salad cookbook photo: 19 ingredients, "2.5 can"/"handful"/notes all correct). Only the live CAMERA capture (no camera on the sim) hasn't run. | Camera framing/orientation quirks on a real phone are unverified. | Test capture-from-camera on a physical device. |
| 2026-08-31 | import/ui | **No way to DROP an ingredient at reconcile** (feature). e.g. "Crackers, to serve (optional)" the household doesn't want — should be removable entirely. And if the dropped ingredient is referenced in the steps, its chip must demote to plain text (no dangling chip pointing at a gone line). | Optional/unwanted ingredients get committed anyway; the user has to live with them. | Add a "remove line" affordance to the reconciliation card; on commit, drop the line and rewrite any step `ref` to that line_index into a plain-`text` token. |
| 2026-08-31 | docs/design | **The `design-board.html` "Import & recipe view · v3" section is stale.** The shipped UI diverged from those frames during live iteration — the flow merged to a single editable "Review recipe" screen (no separate triage), rows expand-in-place to a full edit card, band-based auto-lock, ingredient/unit "did you mean" chips, fields greyed-until-matched, allowed-unit enforcement, Save gated on all-valid, "to taste"/imprecise handling. | A contributor designs against stale frames; the board misrepresents the real UI. | Refresh the v3 board section to match what shipped (or re-shoot the frames from the running app). `design-board.html` is the single source of truth; `import-ux-redesign.md` is ephemeral scratch (now flagged as such) and is NOT kept in sync. |
| 2026-08-31 | import | **Cup-measured produce all flag "pick a supported unit"** — mango/tomato/onion/avocado in cups have no density, so each needs a manual unit pick at reconcile. | Extra taps on nearly every produce import; reconcile friction. | Seed densities for common produce, or pre-select the produce's own count-measure as the default. |
| 2026-08-31 | import | **`prefillStubFromUsda` background job not triggered** — a client-created import stub syncs up but nothing server-side enriches it from `usda_food`. | New-ingredient stubs stay bare (no density/macros) until hand-filled. | Wire a server trigger / edge job on stub insert to run the existing `prefillStubFromUsda`. |
| 2026-08-31 | evals/import | **Residual unit-accuracy ~88% + a few gold ambiguities** — uniform across providers (⇒ scorer/gold, not the model): the scorer requires exact unit-string + `unit_mappable` equality (clove vs piece, both "mappable", still fails); flagged gold calls await owner review (green-goddess zest→notes, dense-bean tofu substitution note, mint-pea "thawed"). | Benchmark under-credits correct extractions; a few gold lines may need re-labelling. | Teach the scorer mappable-equivalence; owner reviews the flagged gold lines. |

_When empty, keep the header — an empty tracker is a healthy signal, not a file to delete._
