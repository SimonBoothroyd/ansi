# Tech debt tracker

Known, deliberate debt. Pay it down in small, continuous increments rather than
letting it compound. Add an item when you knowingly cut a corner; remove it when
you fix it.

| Added | Area | Debt | Cost if ignored | Plan |
|-------|------|------|-----------------|------|
| 2026-09-02 | app/test | **The write-guard invariant is held by a regex over source text.** `no_bare_repo_write_test.dart` derives the write set from the repository interfaces (so a new method is covered the day it is declared) and walks enclosing parentheses to decide whether a call sits inside `ref.write`. It can be defeated by an aliased receiver nested inside something other than the guard, by a stored function reference (`final f = repo.deleteSection; f(id);`), and by a string literal containing parentheses. Six allow-list entries carry deliberate exceptions with reasons. | A determined author, or an unusual call shape, slips a silent write past CI. | The exact rule as a `custom_lint` in `packages/ansi_lints/` resolves the invoked element instead of matching text, and puts the squiggle in the editor. It would be the repo's first in-house pub package and must respect the load-bearing `custom_lint 0.8.x` / `riverpod_lint <3.1.0` pin — worth it only if the regex is actually evaded. |
| 2026-09-02 | sync/ui | **The sync banner has never been seen against a real stalled stack.** The four states, the thresholds, the copy and the two quiet lines are unit- and widget-tested over an overridden provider, and the connector's dropped-write report is asserted against a real `ps_crud`. But nothing has driven `make test-sim` with uploads genuinely failing, so the *escalation* — five minutes of upload errors turning into a banner, and `Try now` reconnecting — is proven only in the small. | A wiring mistake between `statusStream` and the banner would show as an app that stays quiet in exactly the case the front was built for. | This is the same errand as the existing "offline-queue drain" row: an airplane-mode → reconnect scenario. The surface to assert now exists, which is what was missing before — drive the sim with the local PowerSync container stopped and read the Shop line. |
| 2026-09-02 | sync | **A dropped write is reported, not recovered.** The banner names the table, the operation and the server's code, and "I've seen this" retires the notice — it does not re-queue anything, because the server refused the write and would refuse it again. Re-saving what the write changed is the only fix, and the user has to know what that was. | Someone taps through the banner believing it repaired something. | Only worth more if a drop is ever seen in the wild. The honest upgrade is naming the *row* rather than the table (`"Chicken Curry"`, not `recipe`), which needs a local lookup by id at report time. |
| 2026-09-02 | planning | **A week is a position now, but three of its neighbours are still deferred.** The redesign shipped the viewed-week provider, the header switcher (chevrons + menu) and Cook/Shop following the viewed week (D2/D3). Deliberately NOT built: a **calendar or month view** (refused outright by spec §4 — it invites the per-day planning this app doesn't do); **week templates** ("start from a template" — no data model, no evidence we want one, and `copy last week` covers the real case); a week **archive or lock** (a past week is editable, because every rule about *when* a week would lock is wrong for someone catching up on a Tuesday). `week_plan.label` exists, is always null, and is the natural home for a user-named week if the switcher ever wants one — don't delete it. | Nothing broken; these are refusals, recorded so they aren't re-litigated per screen. The only live cost is that a week far from today takes several chevron taps to reach. | Revisit only on real demand. If a jump-to-an-arbitrary-week need appears, the cheapest honest answer is a date entry in the switcher menu, not a calendar. |
| 2026-09-02 | planning | **Per-week cook-day overrides are still unbuilt**, and the week redesign made the gap more visible: a presentation dish row now PRINTS the cook day (`cooks today · batch of 4`, `from Monday's batch`) read off `buildCookPlan`, so the always-earliest-covered-day default is now stated on the week as well as on Cook. (Pre-existing row above: "Cook-day adjustment is display-only".) | A cook who wants to batch on Sunday instead of Monday now reads a wrong day on two screens rather than one. | Same fix as the existing row — a persisted per-session override store; the week's markers would read it for free, because they are a pure read of the plan. |
| 2026-09-02 | planning | **Retired by the week redesign, recorded so nothing resurrects them:** `_EmptyWeek` and `_LastWeekReference` (the blank-week page and its frozen copy of last week), `_DishMenu` (the per-row `⋯`), `edit_eaters_dialog.dart` (deleted — folded into the entry sheet), the `Shared`/`Per-person` segmented toggle and the `⇄ shared` per-entry tag (redundant once the lens dims instead of filtering), Cook's `_EmptyCookPlan` full-bleed page, and Shop's full-bleed empty page. The house rule that replaced them is written into spec §5 (D5b). | Re-adding any of these re-introduces a screen that swaps itself out for a data condition, which is now a stated rule rather than a per-screen judgement. | Nothing to do — this row exists so the next person reading `git log` for "where did the blank-week page go" finds the answer in one place. |
| 2026-09-02 | ingredients/search | **Tier 2 scans the whole live vocab in Dart, with no SQL `LIMIT`** — deliberately, because a cap would silently stop typo tolerance working for whatever fell off the end as a household's vocabulary grew, and the pass only runs when the SQL search already found nothing. Stated bound: correct to roughly **2 000 rows** on a phone. *(This row replaces "Single-word queries get no typo tolerance" (2026-09-01), which is **closed**: the `>= 2 tokens` gate is gone and `searchRank`'s three tiers — exact · every-token word prefix · guarded typo, rendered under a `DID YOU MEAN` header — are the one rule every phone-side search uses. Guards measured against the real 308-row vocab: OSA distance, a **4**-character floor alone and 3 beside a spelled token, edit budget 1 under 8 characters and 2 from 8, floor 0.75. The old row's own suggestion — "min token length ~5, floor raised to ~0.8" — was tested and **rejected by the owner on 2026-09-02**: five characters misses `aoli` → *Romesco Aioli* entirely, and 0.8 loses `brocolli`, `pistachos` and `chikpeas` while preventing none of the wrong-food hits, because length rather than score is what makes a short guess loose. Shipped rule before: **0 of 477** generated one-edit typos surfaced. After: right family at rank 1 for **93 %**.)* | Past a couple of thousand rows every missed keystroke reads the whole table and scores it in Dart, with no debounce in the picker either. A two-person household is nowhere near that; the number is written down so a growing vocabulary degrades visibly instead of silently. | Add an index (SQLite FTS5 with the trigram tokenizer is the obvious one) when a household's vocabulary approaches the bound — not before. Contract: `docs/design-docs/search-and-matching.md`. |
| 2026-09-02 | import/matching | **The server cascade surfaces only ~6 % of single-word typos, and that is a decision, not a gap** (search & matching v1, D4). Measured over 477 generated one-edit typos through the real `matchOne` + trigram matcher: `auto` 0, `suggest` 31, `none` 446. `almnd`, `chiken`, `suger`, `letuce` and `parsely` all return band `none` with **empty candidates**. The phone's rule would surface 84 % of them — but it only ever *offers*, while the cascade *commits* (`auto ≥ 0.85` starts a line resolved), it is symmetric (both sides through the same `normalize`, which is what makes exact matching reliable), and every band threshold was calibrated against 426 eval cases that are not a merge gate. pg_trgm is also strong exactly where edit distance is weak — multi-word drift, `coconut mlk` → 0.67 `suggest`. | An import line with a single-word typo reaches the review screen with no candidates at all, so the human resolves it by hand. Cheap today, because a human reads every import; expensive the day anything commits without one. | **Stated, not scheduled.** Revisit only if the evals show import lines genuinely arriving mistyped — OCR from photos is the plausible source. The narrow exception already taken is diacritic folding (D5): `jalapeno` scored 0.500 against `jalapeño`, under `BAND_SUGGEST_MIN`, and that was a normalization miss rather than a fuzziness question, so both mirrors of `normalize` now fold. |
| 2026-08-24 | scaffold | Version floors in `app/pubspec.yaml` are caret ranges. `pubspec.lock` **is** committed now, so resolution is pinned in practice. | Loosened floors could drift on a `pub upgrade`. | Tighten floors when a resolve conflict actually shows; the lock carries it until then. |
| 2026-08-26 | recipes/ui | Two Forui 0.22.3 workarounds: a custom underline tab bar (Forui's `FTabsStyle` underline needs the un-exported `FVariants`) and a custom hairline (`FDivider` bakes in a 16px margin). | Diverges from Forui; extra code to maintain. | Revisit if Forui exposes an underline `FTabsStyle` label style and zero-margin dividers. |
| 2026-08-26 | books/ui | **Reorder is up/down menu items, not drag-and-drop — now at both levels.** Library v2 gave books the same idiom sections already had (the book `⋯`, plus a "Reorder books" sheet on the Library `⋯`), deliberately: books do not get a drag handle while sections still lack one. | Clunky for many sections, and now for many books. | Add a reorderable list when Forui offers a drag handle, or wrap Material's `ReorderableListView` — **both levels convert in one slice**, and the reorder sheet is the natural first host. |
| 2026-08-26 | books | Provider-lifecycle bugs (the `LibraryActions` crash: `ref` used after an async dialog) were covered by exactly one path — the local-only `make test-sim` smoke. **Largely paid 2026-09-02 (Library v2):** `library_view_test.dart` now drives every Library mutation that crosses a dialog — rename a book, delete it (both refusals and the empty-book confirm), move its contents through the picker sheet, reorder, fold — against fakes, which is what this row asked for. Residual: those widget tests use fake repositories, not the real-DB harness, and the recipe/section mutation flows elsewhere are still sim-only. | A lifecycle bug on a still-uncovered path reaches the phone. | Point the Library widget tests at the real-DB harness if a fake ever hides something; extend the same treatment to the recipe editor's dialogs. Standing rule meanwhile: mutations go through keep-alive repo providers. |
| 2026-08-27 | app/test | Host repo tests need an extension-capable SQLite; macOS's system build omits extension loading, so `test_db.dart` falls back to Homebrew's `libsqlite3.dylib`. **The Linux/CI half is now paid (2026-09-01):** the `app` workflow installs `libsqlite3-dev`, runs `scripts/fetch_powersync_core.sh`, and the real-`PowerSyncDatabase` suite is green on `ubuntu-latest`. The macOS-only residual stands. | A machine without `brew install sqlite` still can't run `make test-app` locally. | Nothing scheduled — CI proves the Linux path continuously. Revisit the macOS fallback if the sqlite3 package ever ships a bundled host library. |
| 2026-08-27 | app/ui | Opening a Forui dialog with the semantics tree live trips a framework assertion (`node.isMergedIntoParent`), so `integration_test/app_test.dart` filters that one assertion. Reproduces in a plain widget test with `ensureSemantics()`. | Real assistive-tech users may hit a malformed semantics tree; the filter could mask a future regression. | Re-check when forui moves past 0.22.x; drop the filter if it's fixed, otherwise report upstream. |
| 2026-08-28 | cook_plan/shopping | **Nudge/shop-list scale gap** (accepted 7.6 edge): the whole-batch nudge advises cooking ×1 while the shopping list keeps buying the honest raw factor (×0.75) — the toggle is display-only by decision (plan 0010). The nudge line now says "shopping still buys ×0.75" so the gap is visible. | A cook who follows the advice under-buys and must top up by eye. | Persist the nudge choice and let the shop list read it (the same persisted per-session override store as the deferred cook-day adjustment) — candidate for 7.7 or step 11. |
| 2026-08-28 | cook_plan/ui | The **whole-batch toggle** on a session tile is ephemeral widget-scoped state (an autoDispose provider): it resets when the tile scrolls out of the ListView or the app restarts. Persisting it was explicitly ruled out of 7.6 (plan 0010 decision log). | A cook re-toggles after scrolling; two devices don't share the view choice. | Revisit only if the reset annoys in practice — a persisted per-session override store is the same machinery the deferred cook-day adjustment needs. |
| 2026-08-27 | cook_plan | **Cook-day adjustment** (step-5 title) is display-only — the timeline handle isn't draggable; cook day is always the earliest covered day. | Can't move a batch's cook day off the default. | Add a persisted per-session override store (the plan is otherwise purely derived) when there's demand; stretch. |
| 2026-08-27 | cook_plan/ui | The freshness timeline (`_TrackPainter`) draws on a fixed Mon→Sun axis with a 7-day ruler. `CookTimelineSpec` is unit-tested (day coords + tails); the painted bar is sim-verified (fresh/split + freezer/blue) but not golden-tested, and multiple meals on one day collapse to a single marker. | A rendering regression could slip; a two-meals-one-day session under-marks. | Add a golden test if the bar changes often; badge multi-meal days if it matters. |
| 2026-08-27 | ingredients | **FAO/INFOODS density fallback — INGESTED** (2026-08-31, step-8 hardening): FAO/INFOODS Density DB v2.0 (638 rows, sha-pinned) wired into the seed pipeline as an audited prefill fallback — 12 rows mapped via an explicit reviewable link file, **30 candidates rejected with reasons** (FAO is thin exactly on this tail: no tofu/tortilla/seaweed/mushroom; its Fruits section is 3 rows), never-overwrite structural (`where density_g_per_ml is null` + contradiction-fails-loudly), R2 bound (0.03–2.0 — real herbs sit at 0.038) beside R1. Coverage **265 → 277/307**, then **→ 296/307** in step 8.5's **D4d** hand pass (19 rows the owner's field test found stranded — liquids and leafy produce — filled by hand rather than by loosening D4c's admission rule). **Today: 297/308** — Canned Diced Tomatoes joined the vocab on 2026-09-01 carrying a portion-derived density, so both halves moved by one and the audited tail is unchanged. | The remaining 11 rows (tofus, vegan proteins, tortillas, herbs-tail…) keep splitting into honest subtotals. | Hand-curate the 30 from other sources as needed — FAO has nothing more for them. **Licence note:** FAO v2.0 is © FAO, non-commercial reuse authorised on request (recorded in `supabase/seed/scripts/fao_density.md`) — revisit only if Ansi ever ships commercially. |
| 2026-08-27 | recipes/ui | **Cook mode** and the **Notes tab** from the step-2 design board are unbuilt. **Narrowed 2026-09-02:** the inline method chips + timers half is paid — import tokenizes a method (step 8) and the editor v2 (plan 0022) writes, edits and inserts chips and timers for every recipe, so cook mode now has one input shape (`methodSteps`, structured timers, derived step keys). What remains is the hands-free view itself and the Notes tab. | No hands-free cooking view; the recipe page still diverges from the board's cook-mode frame. | Build cook mode as its own slice off the board's frame; the token stream is ready for it. |
| 2026-08-27 | recipes | **Recipe photos** are unbuilt. Deferred from step 2 because they needed Storage + auth — step 7 shipped auth, so this is now **unblocked**. | The recipe hero stays photo-less; the design board shows photos. | Supabase Storage bucket + upload/display slice (photos in Storage, not the DB — spec §2). |
| 2026-08-27 | books/ui | **Per-book detail screen** is unbuilt — all books render inline on the Library screen. Deferred from step 3. **Re-worded 2026-09-02:** the original trigger was "when book count makes the inline list unwieldy", and Library v2's fold removed it — a folded book costs two lines however much it holds, and the book `⋯` is the management surface a detail screen would have hosted. | No per-book landing page. Length is no longer the cost. | Revisit when a book wants a page of its **own** — a cover or colour (the one deferred Library v2 idea that would need a migration, `book.color`), notes, per-book search — not because the list got long. |
| 2026-08-27 | shopping | **"Clear list / new trip"** is unbuilt. Deferred from step 6 to step 7 as a sync concern, but step 7 did not ship it. | Check-state and manual top-ups accumulate forever; no way to start a fresh trip. | Add a clear/new-trip action (reset check-state + soft-delete manual contributions), synced like any other overlay write. |
| 2026-08-27 | sync/test | Sync remains untested for: **offline-queue drain on network loss** (airplane-mode → reconnect) and a **true two-client concurrent-edit session** (LWW / independent-row union, spec §3). `uploadData`'s put/patch/soft-delete mapping and fatal/transient handling are now unit-tested, and duplicate shopping-entry merge is repo-tested — those parts of this row are paid. | A regression in reconnect or cross-device conflict handling would slip. | An airplane-mode → reconnect test; a two-client sync scenario (possibly two sim clones against the local stack). |
| 2026-08-27 | auth/dev | **Ghost users after `supabase db reset`** — the ONBOARDING path is now handled (2026-08-31): `ensure_onboarded`'s `23503` (`auth.users` FK) classifies as `accountMissing` and the connecting screen offers an honest **Sign out** (retry stays secondary). Residual: the **cached-household fast path** (`_reconcile`, session.dart) hits the same ghost failure and deliberately swallows it — the device publishes `SessionReady` on the stale cached household and shows an empty-but-working app, because flipping a live session into an error from a background reconcile would break the offline-first contract (the cached household stands when the network disagrees). | A dev signed in *before* the reset sees an empty app, not the sign-out offer, until they sign out by hand. | **Narrowed 2026-09-02:** the banner this row asked for now exists — `AnsiSyncBanner`, hosted once by the tab shell above the routed child, advisory and never a state flip. What is left is only the hook-up: give it an `accountMissing` case fed by `_reconcile`. |
| 2026-08-28 | pickers/ui | The ingredient picker's "Recent" section derives from live usage (recipe lines + manual top-ups), not a selection history — picking something and then deleting its line drops it from Recent, and a fresh household shows the alphabetical list instead. | Recents are slightly less sticky than a true history. | Add a local selection-history store only if the derived feed annoys in practice. |
| 2026-08-29 | seed/usda | `seed_usda.sql` regenerated on **Foundation 2025-04-24** (the previous file came from a different Foundation release: 8262 → 8204 foods, macros refreshed for revised rows). All 248 `usda_links.jsonl` links resolve in the new bundle; unlinked dropped foods only shrink the create-search reference. | A future regeneration from yet another release churns the file again; linked macros can shift slightly between releases. | Record the bundle version in the generated header next time the generator is touched; re-verify links on every regeneration (the seed's trailing check already fails loudly). **Added 2026-09-03:** the file is 8,204 plain inserts into a primary-keyed table, so it is **not re-runnable** — the deploy button's reseed skips it when `usda_food` is populated, and refreshing the reference on cloud now means a migration that upserts (data is durable since 2026-09-03; a reset is an owner call, cloud-setup §2c). |
| 2026-08-29 | ingredients/ui | **The 7.8 density entry has no `make test-sim` scenario** (g/ml ⇄ "a spoon weighs…", the volume-label redirect, the live chip unlock) — it is covered by widget/repo/unit tests only. **Narrowed 2026-09-01:** the 8.5 **manager** half of this row is paid — scenario 5 opens Library ▸ Ingredients, works the stub band, renames (asserting the D6 `match_text` rewrite in the local db), proves a filled-in form does not promote the row, and adds by barcode through the typed field, so the real-stack legs (RLS on the ingredient update, `allowed_units` round-trip, the server-side USDA prefill + density triggers) do run. | A regression in the density-entry wiring — the one path where a single stored fact is written from two phrasings and immediately changes what units the row admits — would surface only on-device. | Fold the density entry into scenario 5 rather than writing a sixth: it is two more taps on a form the scenario already has open (enter a spoon weight, assert the stored g/ml and the chips it unlocks). |
| 2026-09-01 | ingredients/device | **The camera legs are one physical-device errand (step 8.5).** Nothing with a lens has run: `mobile_scanner` 7.4.0 compiles its still-image `analyzeImage` out on the iOS Simulator, and a camera-less sim never *errors* — it waits forever — so `errorBuilder` never fires either. The whole slice is one sitting on a real phone, checkboxes: ① **scan-from-photo** — build it *and* verify it (ruled to this slice in plan 0020 batch 4, not a sim-only error state); ② photo-import **camera capture** (`ImageSource.camera` beside the gallery picker — shares this row with the step-8 import gap); ③ a **live barcode scan** off a real pack; ④ the **camera-denied** notice actually rendering after a permission refusal; ⑤ the **G2** density-row overflow and the **D4c stranded-default** flag, both visual checks on a real screen rather than a 402pt sim. | The one entry point designed around hardware is verified only through its typed sibling. A permission or framing bug reaches a user first, and scan-from-photo does not exist at all. | One device session, in that order — ② and ③ share the camera plumbing, ④ falls out of ③ by refusing the prompt, and ⑤ is two screenshots while the phone is already in hand. |
| 2026-09-01 | app/test | **A second framework assertion is filtered in `integration_test/app_test.dart`** (2026-08-27's forui semantics filter was the first): a popped form's still-focused field can fire `EditableTextState._scheduleShowCaretOnScreen` a frame late, against a defunct element. Filtered narrowly by its stack, beside the 7.7 `_updateSelectionRects` filter it mirrors. The mechanism matters beyond the noise: **that callback scrolls the enclosing viewport**, so a late one can park the just-returned list at an arbitrary offset — which is why scenario 5's post-return assertions scroll rather than wait. | Two filters now; each could mask a real regression in the same family, and a third would mean the suite is quietly ignoring a class of error. A **user-visible** symptom is plausible on the same mechanism, though unobserved: pop a form with the keyboard up and the list behind it can come back oddly scrolled. Cheap to dismiss, worth naming honestly. | Re-check both filters on every Flutter/forui upgrade — delete the one that stops reproducing. If the user-visible scroll jump is ever seen for real, the fix is unfocusing before the pop, not a wider filter. |
| 2026-08-28 | recipes | The `Recipe` aggregate doesn't carry `favorite`; the recipe page resolves the flag from the list-summary stream instead (an extra watch, and `saveRecipe` can't clobber the flag — by design). | Mild indirection; a second subscription on the recipe page. | Fold `favorite` into the aggregate if the page grows more list-summary needs. |
| 2026-08-31 | import/ui | **Review-screen polish, remainder.** Three rough edges from the first live drives are still open: the amount editor lists an ingredient's whole measure set (~13 chips for Tomato, ~9 for Red Onion) with no prioritisation or "more" fold; the "from source" reference line dups the amount ("2–3 cloves garlic **cloves**, sliced"); and a no-quantity/imprecise item dumps its whole raw line into the amount slot ("Tortilla chips (to serve (optional))") instead of a blank/"—" with the qualifier in notes. | Rough first-cut UX at the exact moment the user is deciding whether to trust the import. | Polish pass: prioritise/collapse the unit chips behind a "more", de-dupe the source line against the resolved amount + ingredient text, blank-not-raw amounts. *(The two worse items from this batch — method chips dropping the ingredient label, and the "N to review" header not decrementing while the Save button's counter did — were **fixed in the step-8 hardening pass**, along with the validation flicker.)* |
| 2026-08-31 | import | **Photo path: no in-app camera capture, and no real-device run.** The picker path is sim-verified end to end (pick → crop/rotate → client downscale → Claude vision → reconcile, on a Dense Bean Salad cookbook photo: 19 ingredients, "2.5 can"/"handful"/notes all correct). But intake is `image_picker`'s `pickMultiImage()` — the **gallery** picker — so "photograph the page" is really a two-step: shoot in the Camera app, come back, pick. There is no `ImageSource.camera` path, and nothing has run on a physical phone. | The natural gesture for a cookbook page (point and shoot) isn't offered; camera framing/orientation quirks on a real phone are unverified either way. | Add a capture option (`pickImage(source: camera)` beside the picker, or a multi-page capture loop) and drive one import from it on a physical device. |
| 2026-09-02 | import | **The nested-recipes import leg is host-tested only, never sim-driven.** 8.6 shipped the "↪ your recipe" offer chip, the linked-line save gate and the Review MAKES row (frame e/h), but the canned sim payload carries no `recipe_candidates` (there is no stable local recipe id to point one at) and the real matcher lives behind the edge function — so scenario 6 exercises the editor path only. | A regression in the review-side linking (chip render, unlink, Save arithmetic, `yield_raw` prefill wiring) would pass `make test-sim` and only surface on a live import. | Either teach the canned-payload path a late-bound candidate (inject a seeded recipe id at scenario runtime) or fold a live edge-fn import into the next physical-device/cloud errand alongside the camera legs. Host tests pin every branch meanwhile (commit `2d8f53b`). |
| 2026-09-02 | import | **Method-*step* sub-recipe mentions stay plain text.** 8.6 v1 links *ingredient lines* (`sub_recipe_id`); a mention of a component inside a method step still renders as prose — the 0014 `ref`-token variant pointing at a `recipe_id` was not built. | A tokenized method can chip its own ingredient lines but not "the aioli" as a recipe; no navigation from step to component. | Small slice on top of 8.6's plumbing: a step token variant carrying `recipe_id`, rendered with the shipped `RecipeChip`. Wants the cook-mode work (also tracker) as its natural moment. |
| 2026-09-02 | test-sim | **Scenario 2 flaked once in eight runs** at `find.text('3 clove')` after re-opening a saved recipe (measure-label timing); passed on re-run and in every other run. | A rare red that erodes trust in the suite if it recurs unexplained. | Watch; if it recurs, pin the measure-label emission (likely the first PowerSync watch tick racing the page open) rather than padding timeouts. |
| 2026-09-01 | seed | **Canned Diced Tomatoes has no `can` measure** — `seed_measures.sql` is generated and the USDA CSV bundles aren't on this machine; the row works (oz/g/volume via density) but "1 can" lines need a manual pick. | A can-measured import line flags until picked. | Fetch the pinned FDC releases, add the `add_measure` override, `deno task gen-measures` — the churn guard from batch 6 applies. Then `reseed_template` and run `supabase/rollout_measure_refresh.sql` (cloud-setup §2b, plan 0023): the seed lands on the template only, and the measures rollout is what carries the new `can` onto the existing household without touching its own measures. |
| 2026-09-01 | naming | **Mise → Ansi rename — SHIPPED (2026-09-01), all three tiers.** Tier 1 display labels + docs, tier 2 the Dart package (`package:ansi/`) and every identifier (`AnsiColors`/`ansiMono`/`AnsiConnector`…) and file name, tier 3 the OAuth scheme (`io.ansi.app://login-callback`) and both bundle ids (`io.ansi.app`) — merged as `b01044b`. What is left is **fossils, not work**: (1) the repo **directory** on disk is still `~/projects/mise` — a `mv` outside version control, owner's to run, nothing in the repo depends on the path; (2) the **Supabase cloud project's display name** is still the old one — cosmetic dashboard text; the project ref, which is what everything actually resolves, never contained a name; (3) **memory-note filenames** (`[[mise-*]]`) in the owner's agent memory store, deliberately kept because notes are cross-referenced by filename and live outside this repo; (4) **history** — `completed/` plans, ADRs, this tracker's own dated rows, and the `schema` tag inside already-captured `evals/runs/*.json` (never validated on read; new runs write `ansi.eval.*`). A finished record is not rewritten to match a later name (`docs/README.md` rule 3). | Low, and bounded. A reader hits "Mise" in history and wonders; the fossils are named here rather than chased. | Nothing scheduled — this row is a map, not a queue. **One live follow-up, not debt:** the OAuth scheme cutover is half done — the cloud dashboard lists **both** redirect URLs and no sign-in has been walked on an `io.ansi.app` build, so the old URL stays listed until that first success. [`docs/cloud-setup.md`](../cloud-setup.md) §1.6 + checklist row 2. |
| 2026-09-01 | seed | **`seed_measures.sql` cannot be regenerated without the FDC CSV bundles**, which are ~40 MB and deliberately not committed (`gen_measures.ts` exits if the dataset dirs are missing). Two things ride on this: **Canned Diced Tomatoes ships with no `can` measure** — its FDC record (333281) and a `seed:typical` `add_measure` are both ready, but applying either means re-running the generator; and **"crushed" is held out of the normalizer's canned-phrase cut-word set** (`CANNED_CUT_WORDS`) because `Canned Crushed Tomatoes` holds the generic `tomato canned` key that the committed SQL keys two `can` measures on, and the file's own guard raises at `db reset` if that match_text stops existing. | A `1 can diced tomatoes` line has no `can` chip to pick, so it flags "Pick a supported unit" with no one-tap fix — the exact shape the amount editor's measure pre-select exists to avoid. And the cut-word rule reads as arbitrary until crushed joins it. | Fetch the pinned Foundation + SR Legacy releases, `deno task gen-measures`, then in one commit: add the `can (14.5 oz)` / `can (28 oz)` overrides for `tomato canned diced`, add `"crushed"` to both mirrors' cut-word sets, and re-key Canned Crushed's overrides + link onto `tomato canned crushed`. Each step is a line; the bundles are the whole blocker. Once regenerated: `reseed_template`, then `supabase/rollout_measure_refresh.sql` (cloud-setup §2b, plan 0023) rolls the new `can` measures onto the existing household — insert-missing, so nothing the household authored moves. |

| 2026-09-02 | seed | **Ginger's `piece, 1 inch` weight is a `seed:typical` estimate awaiting the owner's confirmation.** The owner asked for a countable piece of ginger (plan 0022 / [ADR-0010](../decisions/0010-piece-is-an-admission-fact.md)); USDA's only portion is a 2.2 g slice — a thin 1"-diameter round, not a 1-inch piece — so the new measure carries its own 12 g figure from the usual 10–15 g band rather than a borrowed one, and the label is open to "2 cm" if the owner prefers that phrasing. Every other ginger number is USDA-sourced. | A ginger line resolved through this measure is ±30% on one ingredient's macros — small, but it is the one weight in the seed nobody has checked against a real thumb. | Weigh a thumb of ginger; adjust the `add_measure` override's `basis_amount` (and the label, if "2 cm" wins) in `supabase/seed/curation_overrides.jsonl` and reseed. A one-line change either way. |
| 2026-09-02 | books/search | **Library search is recipe TITLES only** (Library v2 / D2, said out loud on the board). "Recipes with almonds" is a different query shape — a `recipe_line_item → ingredient` join, and after 8.6 a *transitive* one through `sub_recipe_id`, which is where "do a component's ingredients count?" becomes a real question — and a hit on a field the row does not display needs a "matched: almonds" line to explain itself, which is a new row anatomy. | You can only find a recipe by a word in its name. Searching for what you have in the fridge silently returns nothing rather than saying it isn't supported. | Its own slice: the join (with an explicit ruling on transitive component ingredients), plus the row line that explains the hit, in the import review's "did you mean" chip idiom. |
| 2026-09-02 | app/search | **`search_query.dart` is a shared matcher living inside one feature's `domain/`.** `features/ingredients/domain/search_query.dart` is now imported by `planning`, `import` (×3), `recipes` and `books` — five features reaching into a sixth's domain for the app's one search predicate. | Exactly how the Dart/TS singularizer divergence started: a shared rule with a feature's name on it invites a second copy nearer the caller. | Move it to `core/search/` with `normalize.dart`'s search-facing half, rewriting imports — no behaviour change, one commit. Do it *with* the plural fix above rather than as a bare move. |
| 2026-09-02 | deps | **mobile_scanner applies the legacy Kotlin Gradle Plugin** — Flutter warns future versions will refuse to build such plugins. Flutter is pinned exactly (3.47.1), so it cannot surprise us. | A future Flutter bump fails the Android build until the plugin updates. | Whoever bumps the Flutter pin checks mobile_scanner's changelog for Built-in-Kotlin support first (upstream is actively maintained). |
| 2026-09-02 | nav/device | **The Android half of Navigation v2 is untested.** `android:enableOnBackInvokedCallback="true"` is set and the shell's `PopScope` (back on a non-Library tab → the Library tab, then exit) is covered by widget tests against the platform channel — but nothing has run on an Android device or emulator. Two specific things the host tests cannot see: that `canPop: false` still lets the system's predictive animation start, and that a back at the Library tab previews the launcher with the `PopScope` present. | The one platform whose back button is a first-class control is verified only in a simulated binding. A predictive-back regression reaches a user first. | One API 34+ emulator session, alongside the physical-device camera errand already on this tracker: drag back from Shop (lands on Library, no exit), drag again (launcher preview), and pop a pushed recipe to confirm the real Android-U transition rather than the FadeForwards fallback. |
| 2026-09-02 | recipes/ui | **Line ergonomics inside a group are still unbuilt** (0022 D7, deliberately out of scope): a line cannot be reordered within its group, and cannot be moved between groups from the UI. | A mis-ordered ingredient list can only be fixed by delete + re-add — which, since 0022, also means re-pointing every chip that referenced the deleted line. | Pure UI: `saveRecipe` **already** persists a changed `group_id` (`recipe_repository_impl.dart`), and the step cards' `▲ ▼ 🗑` is the shipped idiom to copy. Buttons, not drag — `ReorderableListView` is Material. |
| 2026-09-02 | recipes/ui | **A long method is a very long form** (0022 D7, flagged not built): a step cannot be duplicated, and there is no way to collapse a step card while editing — a 12-step import is twelve full-height cards in one scrolling `ListView`. | Editing step 11 of an imported recipe means a long scroll past ten cards you are not touching; the focused card's toolbar and preview make it longer still. | Watch it on a real import first. If it bites: collapse an unfocused card to its first line (the card already knows whether it has focus), and add a duplicate action beside `▲ ▼ 🗑`. |
| 2026-09-02 | recipes | **A step's stable key is derived from its prose, so identical steps collide.** 0022 D8 wanted a stable step id for cook mode ("you are on step 4" surviving a rebuild, a sync or a resume). `MethodStep` did **not** get an `id` field — the `steps` jsonb is the frozen §4.6 contract and a new key would land in every row the import commit writes — so `stableStepKey` hashes the step's own text instead. It survives a reorder; two steps with byte-identical prose do not tell apart. | Nothing today (cook mode is unbuilt). When it lands, a recipe with two identical step sentences could resume on the wrong one. | Decide it with cook mode, which is the only consumer: either accept the collision (rare, and the cost is one step of scroll), or add the id to the wire format in the same change that touches §4.6 for the `ref`-to-a-`recipe_id` token variant — one contract change, not two. |
| 2026-09-02 | books/test-sim | **Library v2's new surfaces are widget-tested, not sim-driven.** `make test-sim` scenario 2 walks the header split (New recipe under `＋`; scenario 5 takes `⋯ ▸ Ingredients`), the in-card new-section row and the scoped finders — but not the fold, the pinned title search (nor its `DID YOU MEAN` band), the book `⋯` menu, the delete refusal with "Move them to…", or the reorder sheet. | A regression in those flows on the real stack (a sheet on the wrong navigator, a fold key surviving sign-out) would surface on a phone first. | Extend scenario 2 by four taps once it is next re-driven: fold and unfold a book, type a query and clear it, rename a book through `⋯`, and open the refused delete; keep it under a minute. |
| 2026-09-03 | ingredients/normalize | **The `(ch|sh|x|z|s)es$` rule also mangles genuine plurals whose singular ends in `-se`** — `cheeses` → `chees`, `pulses` → `puls`, `mousses` → `mouss` (found by lane D of plan 0023 while fixing `molasses`; a different class — a real plural, wrongly stemmed — so deliberately not folded into 0022). Both mirrors are bug-compatible and no vocab row hits it today; a household-typed "cheeses" stub would carry `chees`, self-consistently. Related, same shape: `cookies` → `cooky` via the `-ies` rule (five `usda_food` rows). | A stored key that is a non-word is harmless while both sides agree; it becomes a stranded row the day one side is fixed alone. | Fix on BOTH sides in one slice with a whole-word `match_text` rewrite migration in the 0022 pattern (template pre-checks raise on a unique-index collision); the invariant-word set is not the tool — this needs an `-ses`→`-se` rule with its own exception list (`glasses`, `classes`, `molasses` already invariant). |
_When empty, keep the header — an empty tracker is a healthy signal, not a file to delete._
| 2026-09-03 | seed | **`seed_measures.sql` was HAND-PATCHED with one row** (plan 0024 seam D1): `yellow bell pepper` carried only `pepper, large` (186 g) where the other three bells carry a 119 g `pepper, medium`, so the curation borrows the medium from the red bell's FDC food. Its `add_measure` override is committed like every other, but the generator cannot run here (the FDC bundles are the blocker above), so the row was written into the generated file **at exactly the position and in exactly the format a regeneration would emit** — after `pepper, large`, `sort_order 0`, the same `— borrowed` source string the tier-2 borrows use. | A generated file with a hand-written line in it is a lie waiting to happen: the next regeneration is the moment to find out whether the two agree, and nothing checks it in between. | Diff the file after the next `deno task gen-measures` (the same errand the bundles row above already needs) and confirm it comes back byte-identical. If it does not, the override is what is wrong, not the file. |
| 2026-09-03 | supabase | **`0023`'s backfill list is a frozen snapshot of the default-measure curation.** `ingredient_default_measure_backfill()` embeds the 132 `(match_text, label)` pairs as of 2026-09-03 so it can reach existing households without a reseed (data is durable, cloud-setup §2c); the source of truth stays `curation_overrides.jsonl`, and the template gets its values from `seed_curation.sql`. | A row re-ruled later (or a newly measured ingredient) reaches new households through the clone and the template through a reseed, but never reaches an existing household — the function will happily fill the *old* answer into a null. | A change to a `default_measure` ruling needs a fresh operator statement (or a small migration) alongside the reseed, exactly as an `allowed_units` re-ruling does. The function is fill-only, so it can never overwrite a household's own choice — the failure mode is a missing fill, not a wrong one. |
| 2026-09-03 | import/ui | **The review screen's method editor cannot mint a NEW line** (seam D4's stated scope cut). The chip picker's `＋ Add an ingredient to this recipe` is drawn disabled with its reason: a brand-new line would need a flat index `buildCommit` does not walk, and the review screen has never had an add-a-line affordance. | A method that mentions something the ingredient list is missing cannot be chipped at review — the user saves, opens the recipe editor, adds the line and chips it there. | Only worth building if it is actually wanted. The honest shape is minting an index past the payload's last one and teaching `buildCommit` to walk the union, which is a real change to the commit contract rather than a UI flag. |
| 2026-09-03 | recipes/ui | **A "needs a weight" marker on the recipe page routes to the EDITOR, not to an amount sheet.** Seam D5 makes every named line a door, and three of the reasons open exactly what they should (the flesh-out form, the sub-recipe). The two LINE-shaped reasons — `needs a weight`, `no amount` — push `/recipes/:id/edit` instead, because the recipe page is a read-only surface with no writer for a line amount. | The one-tap fix the board drew is two taps and a screen change for the commonest reason of the five. | Give the recipe page the shipped quantity sheet for a single line (it already has the ingredient and its measures), or accept the editor hop and say so on the marker. Wants a ruling on whether the read-only page should write at all. |
| 2026-09-03 | recipes/planning | **The per-week recipe override is designed for, not built** (plan 0025 item 6, owner-ruled D6b "exclude for now"). The owner wants, later, to edit a recipe *for one planned week only* — tick an optional line back in, substitute an ingredient. What exists: every derivation (macros, shopping; the cook plan reads only component lines) runs its lines through one pure `effectiveLines(lines, planEntryId)` seam in `features/recipes/domain/effective_lines.dart`, whose only rule is "drop `optional`" and whose `planEntryId` is accepted and deliberately unread (a domain test pins that passing it changes nothing). What does not: a `plan_entry_line_override` table (`plan_entry_id · line_item_id · included · substitute_ingredient_id · quantity · unit · measure_id`, one row per touched line, the untouched recipe stays the recipe), the join in the seam, and the "Used in" tab surface where a week-specific edit would show. | Until then an optional line is out of every week's totals and list, and the only way to buy the lime is a manual top-up on Shop. The shopping repo also applies the seam per RECIPE, before sessions — a per-entry override will need the entry-to-session mapping the cook plan already holds. | One migration + the join in `effectiveLines` + a reader in the shopping derivation that has the entry in hand; nothing in macros or the page should need to change. Do not add a second filter anywhere — the seam is the point. |
| 2026-09-03 | test-sim | **The seam slice is host-tested only — no simulator ran.** `integration_test/app_test.dart` scenario 4 was edited for D2 (a sized-produce line now arrives unflagged, and the commit's `measure_id` is asserted in the local db) but `make test-sim` was not driven: this lane had no booted sim. Scenario 4's method-edit leg and scenario 5's "Counts as" leg from `seam-impl.md` §7 are **not** written either. | The one gate that runs the real stack has an unverified edit in it, and two D4/D1 legs the sketch asked for are missing. | Boot a sim, `make db-up`, `make test-sim`; then add the two legs (type into step 2 at review and assert the saved `refs`; set Counts as on a measured stub, reopen, assert it stuck). |
