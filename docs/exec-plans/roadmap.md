# Build sequence & status

Ordered per spec §7 (reordered around the meal-planning core). Each step lands
with tests before the next starts. Update the Status column as you go — this is
the roadmap agents and humans read first.

| # | Step | Where | Status |
|---|------|-------|--------|
| 1 | **Unit + ingredient data model** — families, conversions incl. density | `app/lib/core/units`, `supabase/migrations/0002` | 🟢 done — unit system (tested); data model (`0001`/`0002` + RLS); 291-ingredient household vocab (mined + curated, variety-level, canned/dried forms); `usda_food` reference (8262 foods, macros incl. fiber); prefill → 248 `complete`, 43 honest `stub`. Density still sparse (FAO/INFOODS fallback deferred → [tech-debt tracker](./tech-debt-tracker.md)) |
| 2 | Single-user recipes — create, group ingredients, scale, recipe page | `app/lib/features/recipes` | 🟢 done — Forui UI matching the design board (list · recipe page w/ live servings scaling · editor · inline vocab picker), real bundled fonts; `0003_recipes.sql` (recipe/group/line-item + RLS); PowerSync persistence (local-only until step 7's `.connect()`); bundled 291-ingredient vocab loader (retired in step 7 — vocab now syncs); verified end-to-end on the iOS sim. **Deferred:** shelf-life editor inputs (→ step 5), macros row (→ step 9), cook mode + inline method chips/timers and the Notes tab (→ [tech-debt tracker](./tech-debt-tracker.md)), books/sections (→ step 3), recipe photos (was blocked on Storage/auth — unblocked by step 7's auth; → [tech-debt tracker](./tech-debt-tracker.md)) |
| 3 | Recipe books + user-defined sections | `app/lib/features/books` | 🟢 done — Library home screen (books → user-named sections → recipes, "Unsectioned" bucket); `0004_books.sql` (`book` + `book_section` tables, `recipe.book_id`/`section_id` + RLS); create/rename/reorder/delete sections, create books; recipe editor "file under" book+section picker; recipe hero "Book · Section" breadcrumb; default book auto-created on first run + adopts pre-step-3 recipes (empty-only gate); verified end-to-end on the iOS sim (create section, file recipe, cold-relaunch persistence — all against real PowerSync views). **Deferred:** multi-book many-to-many (spec §8 open question; v1 = one book/recipe); per-book detail screen (all books render inline for v1; → [tech-debt tracker](./tech-debt-tracker.md)); was local-only until step 7 |
| 3.5 | **Test harness + doc hygiene** — repo tests on a real `PowerSyncDatabase` (view-backed, from `schema.dart`), enabled integration smoke behind `make test-sim`, step-done checklist, satellite-doc refresh | `app/test`, `app/integration_test`, `docs/` | 🟢 done — [exec plan](./completed/0004-test-harness-and-doc-hygiene.md). Repo tests open the real schema (host loads the PowerSync core extension via `make powersync-core`); regression test pins that the local tables stay views; the sim smoke drives create-section → create-recipe → file → breadcrumb. Found and fixed a live stale-breadcrumb bug: SQLite dropped the unused book/section LEFT JOINs, so `watchRecipe` never re-fired on a rename |
| 4 | Week planning — single-week grid, multiple entries/slot, eaters, copy-last-week | `app/lib/features/planning` | 🟢 done — [exec plan](./completed/0005-week-planning.md). Week screen (active week = the Monday-first week containing today; seven-day grid of day cards; meals grouped by slot; **Shared/Per-person lens**; blank-week state with "copy last week" + last-week reference). **Two-step add flow** matching the mockup: a recipe picker (search · Recent/Books · "already this week" surfacing · book·section subtitles · "+ new recipe") → a confirm sheet (slot · eaters · **portions stepper**). `0005_planning.sql` (`week_plan` + `plan_entry`, `eaters` JSON array, free-text `meal_slot`, `portions` override resolving spec §8, RLS); `household_member` seeded as a local-only table (two members "Ada"/"Jun" — real table's `auth_user_id` FK blocks server-seeding until step 7); a bottom nav (Library · Week · Cook · Shop — Cook/Shop inert until steps 5–6) in a shared `MiseBottomNav`. Verified end-to-end on the iOS sim (two-step add + portions, per-person filtering, edit eaters live, tab nav, cold-relaunch persistence — against real PowerSync views). **Deferred to step 5:** the batch-aware shelf-life chips + the "same batch" hint (need shelf-life inputs + clustering); was local-only until step 7 |
| 5 | Batch cook plan — aggregate by recipe, shelf-life clustering, adjustable cook day | `app/lib/features/cook_plan` | 🟢 done — [exec plan](./completed/0006-batch-cook-plan.md). The **Cook** screen (third live tab): the derived batch view — the week's meals grouped by recipe, each greedily clustered into **cook sessions** bounded by fridge shelf life (`keeps_for_days`), with a **freezer merge** (a freezable dish's far instance folds into one session — cook once, freeze the far share — rather than splitting). Each session shows cook day · honest scale (`portions/servings`) · "covers…" · a fresh→gone timeline; split + freezer notes explain the why. Pure-Dart `buildCookPlan`/`clusterSessions` (fully unit-tested); `CookPlanRepository` re-derives on any plan/recipe change (no new tables — the plan is derived). Added the **recipe-editor shelf-life inputs** (keeps/freezable/freezer-days), making those step-2 columns load-bearing. Lit up the **planning add-flow batch chips** owed from step 4: "keeps N d" on picker/confirm + a "same batch" hint (reuses `batchHintFor`→`clusterSessions`). Verified end-to-end on the iOS sim. **Deferred:** whole-ingredient scaling nudge (raw factor for now) + interactive cook-day adjustment (display-only; needs a persisted override) — both stretch (→ [tech-debt tracker](./tech-debt-tracker.md)); was local-only until step 7 |
| 6 | Shopping list from cook plan — contributions, aggregation, provenance, check-off | `app/lib/features/shopping` | 🟢 done — [exec plan](./completed/0007-shopping-list.md). The **Shop** screen (fourth live tab): the provenance-aware list, **derived** from the cook plan like the Cook screen. It sums each ingredient's contributions (one per cook session × recipe line, scaled by the session factor), groups by aisle, shows the breakdown ("Flour 500 g · Curry · cook Mon 300 g · Cookies 150 g · +50 g manual"), and holds **check-off on the rolled-up item**. Pure-Dart `aggregateQuantities` (honest summation — sums within a family, bridges mass↔volume only with a density, else two honest subtotals; never invents a number) + `buildShoppingList` (fully unit-tested). Only a **thin overlay** persists (`0006_shopping.sql`: `shopping_list_entry` check-off + free-text; `shopping_list_contribution` manual-only) — the cook contributions re-derive per device, so nothing to reconcile. **Manual top-up** + **non-food items** via the add sheet. Entry lifecycle handles the recipe-deleted-but-checked case (item drops when it has no live contribution; `ingredient_id` never dangles) — pinned by a repo test. Verified end-to-end on the iOS sim. **Deferred:** package-size/whole-unit rounding (→ step 11); "clear list / new trip" (was deferred to step 7 but did not ship there; → [tech-debt tracker](./tech-debt-tracker.md)); was local-only until step 7 |
| 7 | Sync layer — PowerSync + household + offline queue (parallel once 1–6 schema stabilises) | `app/lib/core/sync`, `docker/` | 🟢 done — [exec plan](./completed/0008-sync-layer.md). `.connect()` is live: a sign-in gate (dev email/password now; Google OAuth wired for cloud cutover) → idempotent `ensure_onboarded` RPC (join-or-create a two-person household, clone the starter vocab) → `MiseConnector` (creds + queue drain, soft-delete-safe). `0007_sync.sql` adds onboarding + the `add_household_claim` access-token hook that injects `household_id` into the JWT the sync rules scope by. The three faked-local tables (`ingredient`/`ingredient_alias`/`household_member`, + `household`) flipped to **synced** — vocab now carries server macros; seeders dropped. The four repo write-paths now use the signed-in household (`kDevHouseholdId` has since been removed entirely — tests pass an explicit household). `docker/powersync.yaml` finished (all 13 tables; JWKS/ES256 auth; discrete-field PG). A **`/connecting` gate** holds the app until onboarding + first-sync resolve `household_id` (fixes a `ProviderException` race), and the `io.mise.app` deep-link is registered natively for OAuth. **Verified live — local *and* cloud:** local service replicates + `/sync/stream` returned the household bucket (414 ops); a **cloud** Supabase + PowerSync Cloud instance ran full E2E through the real app on the iOS sim — sign-in → onboard → **bidirectional sync** (vocab down, recipes up via `uploadData` through RLS); client 1.18.0 speaks edition-3 streams. Onboarding/hook pinned by pgTAP; cloud how-to in [`docs/cloud-setup.md`](../cloud-setup.md); tested/not-tested spelled out in the [exec plan](./completed/0008-sync-layer.md). **Not tested:** the Google *browser* flow, offline-queue drain on network loss, two-device conflict; `make test-sim` needs an auth-aware rewrite (tech-debt) |
| 7.4 | **Post-step-7 hardening sweep** — fix the review's criticals (jsonb round-trip, edit tombstones, claim-less first sync, onboarding races) + tighten across all layers | everywhere | 🟢 done — [exec plan](./completed/0012-hardening-sweep.md). Seven parallel workstreams, all merged: connector decodes jsonb columns + always clears `deleted_at` on PUT, `saveRecipe` diffs children (both ends unit-tested against real `ps_crud`); session is a sealed state machine (`refreshSession()` after onboarding, retry/sign-out on `/connecting`, cached-household offline relaunch, queued auth events, sign-out UI); `0008` migration (advisory-locked onboarding, template vocab clone excluding `manual`/`import_correction`, `is_template` grant lock) + 50 pgTAP assertions incl. the access-token hook + an all-13-tables RLS loop; domain honesty fixes (density ≤ 0, unknown units excluded, imprecise dedupe, same-day batch hint, two-device shopping-entry merge) + watch()/filter gaps closed + a structural watch-coverage test; search normalizes queries + matches word boundaries; `make docs` generates `db-schema.md` for real; UI polish (group-name doubling, quick-pick real summaries, glyph rule, `allowedUnitsFor` unit filtering); auth-aware `make test-sim` (3 scenarios, self-provisioning). **Verified on-device** (fresh install, in-app sign-up): vocab (291) + default book sync immediately, restart mints no duplicate book, method steps round-trip as a real jsonb array with no crash, a title edit leaves every child row live. The closing pass re-enabled the smoke's deferred `TODO(step7-fixes)` asserts — method steps, edit-recipe child survival, and week→cook→shop over **live sync** (green ×2). Remaining tails live in the tracker (airplane-mode + two-client tests) |
| 7.5 | Cloud verification & seeding — repeatable, read-only cloud health checks; cloud vocab gets macros; stream YAML versioned | `scripts/`, `docker/`, `docs/cloud-setup.md` | 🟢 done — [exec plan](./completed/0009-cloud-verification.md). Harness shipped (`cloud_verify.sh` read-only checks, committed cloud streams YAML + CI drift check, smoke_auth signup-token/joined-vs-created/teardown); cloud migrated to 0008, seeded (248/291 template rows carry macros), streams deployed, janitored (template member-less), `cloud_verify` 7 ok · 0 fail. **Google OAuth verified end-to-end** on the sim against cloud: consent → deep link → session → onboard → template clone with macros. Found live: the default in-app browser sheet never self-dismisses after the redirect (app signs in underneath) — fixed with `LaunchMode.externalApplication`; dismissal behavior to confirm on the next cloud sign-in (tracker) |
| 7.6 | Ingredient measures & portions — named per-ingredient measures with gram weights ("1 potato, large = 299 g"); batch cook nudge; honest count-food conversions | `supabase/migrations`, `app/lib/core/units`, `app/lib/features/*` | 🟢 done — [exec plan](./completed/0010-ingredient-measures.md). `ingredient_measure` (`0009`: RLS + sync + template clone, 28 hand-curated seeded measures with sources) + pure-Dart `Measure` (count↔**mass** via stored gram weights, honest by construction); nullable `measure_id` FKs on line items & top-ups (stored `unit='piece'` degrades a missing measure to an honest count); `allowedUnitChoicesFor` puts "potato, large (299 g)" in every unit dropdown (editor, add sheet, edit-top-up — the by-id lookup retired that tracker row); `aggregateQuantities` folds measures into the mass subtotal with provenance; **whole-batch cook nudge** ("cook ×1 instead — covers 4 portions · 1 left over", display-level toggle) + **whole-unit shop hint** ("≈ 2.25 → buy 3", beside the honest total, count-foods-with-a-measure only). Verified: 245 app tests, 54 pgTAP, `make test-sim` green incl. a measure through the editor over live sync. **Review follow-up (2026-08-28):** measure provenance (`source`, `0010`) + backfill for pre-0009 households; seed now GENERATED from FDC food portions (183 rows / 118 ingredients, explicit + auto-detected borrows, basis-qualifier guard, 7 `seed:typical` survivors); shopping honesty fixes (unresolved `measure_id` survives edit re-saves, hint denominates in the measure actually used, invalid/unrecognised/contradictory rows degrade visibly — a measure never folds beside a non-count unit) — 256 app tests, 59 pgTAP (green on a dirty dev DB too). **Deferred:** in-app measure editor (tracker; UI face is 7.7's chips), persisted nudge toggle + nudge/shop-list gap (tracker), package-size flags (step 11) |
| 7.7 | Picker uplift — ingredient & recipe selection redesigned (design-board frames first), unit chips riding the keypad | `docs/product-specs/design-board.html`, `app/lib/features/*/presentation` | 🟢 done — [exec plan](./completed/0011-picker-uplift.md). Board frames signed off (top search decided; frame b rebuilt as two states at review), then built: shared `PickerShell`; **ingredient picker v2** (Recent-from-usage section, honest rows — category/has-density/N-measures hints, per-100 macro line, `stub` badge never zeros — add-new manual stub) replacing the sheet in editor + shopping top-up; **quantity + unit-chip sheet** retiring every unit dropdown (chips docked over the keypad — in-sheet, not a native accessory (documented call); live honest conversion line; the `＋` chip opens **manage measures** = the in-app measure editor: humanized provenance, `manual` rows, soft-delete, volume-unit labels rejected); **recipe picker v2 + confirm v2** (Recent·Books·**Favorites** on the new `recipe.favorite` flag with a recipe-page star, day-tagged already-this-week chips, last-planned recency, per-serving macros/incomplete rows, eating footer; combined Day·Slot dropdown + the full batch prose). Data: `0011` (favorite · run-once `backfilled_at` gate — deletions never resurrect · `measure_live_label_uq` dropped, duplicate labels **merge on read** · `macros_basis`); pure-Dart `summarizeRecipeMacros` pulled forward from step 9 (**step 9 shrinks to the recipe-page macro panel**). Verified: 311 app tests, 67 pgTAP (fresh + dirty-db), `make test-sim` green (scenarios re-driven through the new flows, incl. favorites + add-measure). **Deferred:** stub match_text uses the character-level normalizer until step 8; recents are usage-derived (both → [tech-debt tracker](./tech-debt-tracker.md)) |
| 8 | AI/deterministic import — JSON-LD → photo → reconciliation → stub queue | `supabase/functions`, `app/lib/features/import` | 🟡 normalization (§7) implemented + eval-scored; cascade/extraction still stubbed |
| 9 | Computed macros in UI (stretch) | `app/lib/features/recipes` | ⚪ stretch — **shrunk by 7.7**: the domain summation (`summarizeRecipeMacros` — basis-aware line×vocab totals, honest `incomplete` on any stub/unbridgeable line) shipped with the picker uplift and already feeds the picker rows. What remains is only the **recipe-page macro panel** rendering the same summary |
| 10 | Web UI (stretch; near-free with Flutter) | `app/web` | ⚪ stretch |
| 11 | Anti-waste extras (stretch) — freezer batching, monotony warnings, package-size flags | — | ⚪ stretch |

Legend: ⚪ not started · 🟡 scaffolded/in progress · 🟢 done

## Why this order

The unit system underpins recipes, scaling, cook-plan scaling, and shopping-list
aggregation — nothing works until conversions are trustworthy, so it is built
first and hardened with tests. Import (step 8) is deliberately late: it depends
on a stable ingredient vocabulary to match against.

## Open questions (spec §8, low priority)

- ~~Per-entry `portions_override` for big/small appetites, or `|eaters|` only?~~
  **Resolved (step 4):** `plan_entry.portions` — null tracks `|eaters|`, a number
  overrides (the confirm sheet's stepper).
- Can a recipe belong to multiple books? (v1 assumes one.)
- ~~Meal slots: fixed set or user-definable?~~ **Resolved (step 4):** free-text
  slots with Breakfast/Lunch/Dinner offered as defaults (`mealSlotRank` orders
  known slots ahead of custom ones).
- ~~Freezer-aware batching in v1 or stretch?~~ **Resolved (step 5):** built in
  v1 — a freezable recipe's distant instance merges into one cook session
  (cook once, freeze the far share) rather than splitting.
