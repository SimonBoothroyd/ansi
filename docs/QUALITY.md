# Quality bar

A lightweight, hobby-scale version of the "quality score" idea: grade each area
so gaps are visible and tracked over time. Update grades as areas mature. This is
a living signal, not a gate.

Grades: 🟢 solid · 🟡 partial · 🔴 thin/missing

| Area | Grade | Notes |
|------|-------|-------|
| Unit system (`core/units`) | 🟢 | Built and tested; the reference for `core/` style (step 1). Named `Measure`s (7.6, basis-aware since 7.8/ADR-0008) bridge count↔**basis** via stored `basis_amount`s in the ingredient's `macros_basis` unit — honest by construction (bad amounts refuse like bad density; crossing mass↔volume still needs the density). `densityFromVolumeWeight` makes the spoon phrasing ("1 tbsp = 15 g") the same stored density fact. |
| Ingredient data model + vocab | 🟢 | Migrations `0001`/`0002` + RLS, 308-ingredient vocab, macros prefilled server-side (+ label-sourced curation fills). `ingredient_measure` (`0009`+`0010`, basis-aware `basis_amount` since `0012`): 270 GENERATED starter measures over 141 ingredients (USDA-FDC portions via `gen_measures.ts` — GENEROUS extraction incl. fragments/size classes/rescued disguised servings, then the audited **curation pass** `curation_overrides.jsonl`, plan 0013: every drop/add/override with a reason), synced + cloned at onboarding, **backfilled run-once** per household (`backfilled_at`, 0011), watched per-ingredient repo with **merge-on-read** for duplicate labels and the 7.7 **in-app editor** write path (+ the 7.8 density entry: g/ml ⇄ spoon phrasing, volume-labels REDIRECTED into it). `macros_basis` (0011) stores the per-100 basis. **`allowed_units` (0012, ADR-0008)**: explicit per-ingredient admission, materialized at creation (`default_allowed_units()` trigger, Dart mirror + shared test vectors), curated for the template, extended in-app when a density lands. **Amended by [ADR-0009](./decisions/0009-density-unlocks-both-families.md) (`0014`, step 8.5)**: a density unlocks the other mass/volume family whatever the default unit's family — the leg lives in `density_unlocked_units()` (SQL) / `densityUnlockedUnits` (Dart), read by the defaults *and* by a new AFTER UPDATE trigger, so a density arriving **server-side** (reseed, operator backfill, the USDA prefill) now extends the list too; existing rows backfilled by UNION, never re-materialized, so household edits and the seed's curated overrides survive. Density coverage **297/308** (portion-text derivation 7.8 + audit-round curation fills + the FAO/INFOODS fallback took it to 277/307; step 8.5's **D4d** pass filled 19 more — the owner's field test found liquids and leafy produce stranded by D4c's strict rule, and the ruling was *keep the rule, fix the data*; Canned Diced Tomatoes then joined on 2026-09-01 carrying its own portion-derived density; R1 seed assertion: volume-default ⇒ density); the remaining 11 are the audited tail (tracker). |
| Ingredients manager (`features/ingredients`) | 🟢 | Step 8.5, closed out 2026-09-01. The vocabulary finally has a face: `/ingredients` (a pushed route off Library — whole vocab, stub band on top, the picker's honest rows) and `/ingredients/:id`, the long-deferred **flesh-out form** — `canonical_name` (a rename rewrites `match_text` through the newly ported Dart phrase normalizer, and re-fires the server prefill via `0015`), aliases, category, default unit, macros + basis, the shared 7.8 density entry, and the explicit ADR-0008 `allowed_units` list. **Macros gate `complete`, density does not, and confirming is a human act** (plan 0020 D5) — enforced in the repo, not only on the CTA. Delete is a soft-delete **refused with a count** while a live recipe line points at the row (aliases go with it). Admission is **basis-strict** since D4c — the default unit's family is not an admission source, `densityUnlockedUnits`/`densityStrippedUnits` are derived both ways from the same rule, and a row whose stored default violates it is flagged with a one-tap fix rather than rewritten. Covered by widget tests over the real form, repo tests on a real `PowerSyncDatabase`, normalizer tests against **vectors shared with `normalize.test.ts`**, `unit_admission.sql` (61 assertions, 47 of them added by this step, over ADR-0009's defaults, both triggers and the `probe_usda` RPC) — **and, since 2026-09-01, the sim's `ingredients_test.dart` (`make test-sim FILE=ingredients`)**, which opens the manager on the real stack: the stub band over synced vocab → rename → the D6 `match_text` rewrite asserted in the local db → a filled-in form that does *not* promote the row (D5) → add-by-barcode → the saved row's provenance surviving the sync round trip. So the trigger legs finally run against a real device's upload queue. **The open sliver:** everything with a camera in it — a live scan, in-app capture, scan-from-photo, and the camera-denied notice's render — is a **physical-device** errand (tracker); the sim can only walk the typed path. **Polish pass (2026-09-02):** `piece` is an admission fact (ADR-0010) — absent wherever a measure exists, curated by hand for the seed, and the measures editor asks whether `piece` stays when a piece-type measure is added; the picker's search is the shared three-tier `searchRank` with a labelled "did you mean" band (single-token floor 4 by owner ruling; measured 29/30 hand typos found on the real vocab). **No stubs as a side effect (2026-09-03, plan 0025 #3/#8):** every add-new — manager ＋, editor picker, shopping top-up, import review — runs one chain, sheet → flesh-out form → back, with a picker awaiting the form's pop through `pushOnceFor` and re-reading the row, so the quantity sheet opens on the units the form set; the form itself scans a barcode through the same `applyDraft` rule the sheet uses (fill empty fields, keep what a human typed and say so, `off:<barcode>` only where there was no source, confirm nothing). Widget-tested on every host; the sim leg (the import file's create-new walks the form) went green in the split suite (plan 0026 lane A, 2026-09-03). **Macros from a per-serving label (plan 0027 front M, 2026-09-03):** the macros segment gained *per serving* — the four fields take the label as printed, a serving row (amount + g/ml, which is the basis) says what they describe, the stored per-100 derivation (`Macros.per100From`, unrounded) is previewed live with the "label's rounding scales with it" note, and the serving's "1 tbsp = 14 g" / "1 slice = 28 g" is offered opt-in as the row's density (`setDensity`, chips unlock) or a measure (`addMeasure`) in the same Save; nothing new on the wire. Widget-tested over the real form (entry, refusal without a serving weight, both offers off-by-default then landing). |
| Barcode add (`features/ingredients/barcode`) | 🟡 | Step 8.5 lane B. One public door (`scanBarcodeForDraft`) → the scan sheet (camera reticle **plus** a permanently visible typed-number field — the no-permission, scuffed-label, and iOS-Simulator path) → an **on-device** Open Food Facts GET (keyless and free, so neither edge-function reason applies; per-IP rate limits argue for the device) → a pure-Dart mapper → an `IngredientDraft`. The draft **prefills and never completes**: absent panel values stay null with a reason attached, provenance is `off:<barcode>`, ODbL credit rides along, and the row still saves as a `stub` for a human to confirm. Nothing in the module writes a row. Mapper + lookup + sheet + door are tested off committed fixtures with no network, and **the sim's `ingredients_test.dart`** now drives the whole typed path on the real stack — sheet → the real client building the real URL from the typed digits (asserted, so the fixture can't be handed back for a request that never carried the barcode) → the real mapper → the draft → a saved `off:<barcode>` row that survives the sync round trip still a `stub`. The committed fixture and the Dart copy the on-device test carries are pinned to each other by a host test. **Why 🟡, not 🟢:** the **camera** leg has still never run on physical hardware — `mobile_scanner` compiles its still-image `analyzeImage` out on the iOS Simulator, and a camera-less sim never fires `errorBuilder`, so a live scan, scan-from-photo and the camera-denied notice's render are all one physical-device errand (tracker); and no test hits the live OFF API, so a breaking upstream shape change would surface only in a user's hands. **A per-serving panel is no longer a refusal (plan 0027 M-D5, 2026-09-03):** the mapper carries the four printed `*_serving` figures with OFF's numeric `serving_quantity` (+ unit) as a `DraftServingPanel`, never a per-100 guess and never a number parsed out of the free-text `serving_size`; both hosts land it on the per-serving selector — the serving prefilled or flagged "type the serving weight" — and store the derivation, still as a `stub`. Pinned by two new captured fixtures (`peanut_butter_per_serving`, flag flipped on a real capture as its header explains; `kraft_mac_per_serving`, the flag over prepared-only keys) plus constructed serving-quantity variants, and widget tests on both hosts. **The USDA match, said out loud (2026-09-03, plan 0027 front U, `0027`):** the prefill is *named* — `source_label` + `source_score` written by the trigger and by `applyUsdaProbe` beside the `usda_fdc:<id>` stamp — so the form's macros section opens with "Filled from USDA · *food* · FDC id · close match / a guess" read off the row (offline, never a live probe), and two doors: **Not this food** (one write: the density and its D4b strip, the macros, `source = usda_declined`, which the 0015 rename trigger never refills — pinned in pgTAP on both legs) and **Choose another ▸** (`probe_usda(name, limit)`'s next five in the trigger's own order, the pick applied through the same apply with the declined guard lifted; it may replace the prefill's own fill, never a person's numbers, and never confirms). The New-ingredient sheet's USDA leg is the same search — the name is the query, Create applies the pick as the row is made, nothing picked keeps the auto-fill. pgTAP grew to **131** assertions (the label/score on insert and rename, the declined stub on both legs, the widened probe's description/category/order/cap); real-db repo tests cover decline, explicit pick and the guards; widget tests walk both doors and the leg. **Not yet on the sim** (the orchestrator's landing leg) and **`0027` pending cloud**. |
| Barcode add (`features/ingredients/barcode`) | 🟡 | Step 8.5 lane B. One public door (`scanBarcodeForDraft`) → the scan sheet (camera reticle **plus** a permanently visible typed-number field — the no-permission, scuffed-label, and iOS-Simulator path) → an **on-device** Open Food Facts GET (keyless and free, so neither edge-function reason applies; per-IP rate limits argue for the device) → a pure-Dart mapper → an `IngredientDraft`. The draft **prefills and never completes**: absent panel values stay null with a reason attached, provenance is `off:<barcode>`, ODbL credit rides along, and the row still saves as a `stub` for a human to confirm. Nothing in the module writes a row. Mapper + lookup + sheet + door are tested off committed fixtures with no network, and **the sim's `ingredients_test.dart`** now drives the whole typed path on the real stack — sheet → the real client building the real URL from the typed digits (asserted, so the fixture can't be handed back for a request that never carried the barcode) → the real mapper → the draft → a saved `off:<barcode>` row that survives the sync round trip still a `stub`. The committed fixture and the Dart copy the on-device test carries are pinned to each other by a host test. **Why 🟡, not 🟢:** the **camera** leg has still never run on physical hardware — `mobile_scanner` compiles its still-image `analyzeImage` out on the iOS Simulator, and a camera-less sim never fires `errorBuilder`, so a live scan, scan-from-photo and the camera-denied notice's render are all one physical-device errand (tracker); and no test hits the live OFF API, so a breaking upstream shape change would surface only in a user's hands. |
| Recipes (`features/recipes`) | 🟢 | Editor (incl. shelf-life inputs; 7.7 replaced the unit dropdown with the tap-the-quantity chip sheet), recipe page (+ favorite star in the header menu), scaling; **pure-Dart per-serving macro summation** (`recipe_macros.dart`, step-9 pull-forward: basis-aware, honest `incomplete` on any stub/unbridgeable line — a line-less recipe included, never ~0 kcal — feeds picker rows **and, since step 9, the recipe page's per-serving macro panel** — one summation, so the two surfaces can never disagree). Honesty note: 7.8's derived densities, plus the FAO fallback and 8.5's D4d hand pass (now 297/308 vocab rows), turned most `incomplete` badges into numbers; the residual 11-row tail is the FAO tracker row. `saveRecipe` diffs children (sync-safe edits); repo tested against real PowerSync views (incl. computed summaries on both the list row and the page aggregate, the tombstoned-vocab stub case, and favorite round-trip). **8.6 (nested recipes, 2026-09-02):** a line can be a component (`sub_recipe_id`, XOR with `ingredient_id`) resolved through the sealed `ComponentAmount` batch math against a **yield of up to two denominations**; the page renders the component chip, the conditional "Used in · N" tab, and the editor's MAKES row (other-family lock); delete refused with the used-in count; macros fold a resolved component or name the sub-recipe reason via the shared helper. The sim's `nested_test.dart` drives the whole face; the sim pass also caught + fixed the library tree dropping yield columns and the plural-search miss (`0a1bc02`). **Editor v2 (polish pass, 2026-09-02):** the method is step cards with inline chips over real text (tokens ↔ text + marked ranges round-trip byte-equal against the gold), tap-to-edit chip and timer sheets, select → To ingredient / To timer in the platform toolbar, the first-mention amount rule (`ChipAmountRule`, wire format unchanged), tappable identity with the substitution relabel and `keep the old word`, dangling refs pruned on save, convert to plain text. Every recipe now saves `methodSteps`; cook mode has one input shape when it comes. **Round three (plan 0025 #4, 2026-09-03):** the editor's header — TITLE · SERVES · MAKES · TIMES (new: cook / total, minute steppers, unset allowed) · SHELF LIFE · FILE UNDER — is one `RecipeHeaderForm` over a `RecipeHeaderHost`, rendered by the editor notifier *and* the import review; `Recipe` carries `cookTimeSeconds` / `totalTimeSeconds`, the repo reads and writes them (pinned on a real db, including an import-written pair surviving an editor re-save), the page prints them as chips; the edit rules are one pure `RecipeHeaderEdits` extension; a structural test asserts both hosts render every section. **Optional lines (plan 0025 #6, 2026-09-03):** `recipe_line_item.optional` (`0025`) is a first-class line fact — import → sheet toggle → page tag → macros — and every derivation reads lines through one pure `effectiveLines` seam (domain-tested, incl. the unread `planEntryId` that the per-week override will join on); the macro panel names optional lines under the total beside the imprecise ones, and the repo round-trips the flag through the child diff on a real db. |
| Books (`features/books`) | 🟢 | Library, sections, filing; repo tested (incl. watched-join re-fire). Reorder is menu-based, not drag (tech-debt). **Library v2 (polish pass, 2026-09-02):** `＋` creates and `⋯` manages (Ingredients with its stub dot, New book, Reorder, Sign out), a pinned title search on the shared `searchRank` (best tier only, labelled `DID YOU MEAN` when guessed), per-device folds, book rename/reorder/delete refused with a count + "Move them to…", the reporting ★, five empty states; 58 book tests; the sim's `recipe_editor_test.dart` drives it. Reorder is still menu-based; a per-book page is still deferred (tracker). |
| Navigation (`shared/ansi_tab_shell.dart`, `core/router`) | 🟢 | **Navigation v2 (polish pass, 2026-09-02):** the four tabs are branches of one `StatefulShellRoute` under ONE bar; a tab switch is a 120 ms opacity-only content cross-fade with offstage branches once it settles; pushed pages stay top-level over the bar with the platform's own push and back gesture; back on a non-Library tab goes home first (`PopScope`), Android's `enableOnBackInvokedCallback` set; post-action navigations replace instead of flattening; every sheet/dialog opens through `showAnsiSheet`/`showAnsiDialog` on the root navigator; tap-driven navigation goes through `pushOnce`. Three structural tests hold the rules (no per-screen footer, no bare Forui modal, no bare push). `docs/design-docs/navigation.md` is the reference, incl. the self-closing-sheet rule. **0025 D7d:** the bar's selected item steps herb → herbDeep (9.34:1 on the bar's white, 1.88:1 from the unselected muted; was 6.50:1 / 1.31:1), pinned by `test/core/theme/ansi_theme_test.dart` — the lit tab is now the only thing naming the screen on Cook and Shop. **Untested:** Android on a device (tracker). |
| Sync layer (`core/sync`) | 🟢 | `.connect()` is live behind a sign-in + `/connecting` gate (dev email/password; Google wired). `ensure_onboarded` (advisory-locked, template-vocab clone; `0007`+`0008`) + `add_household_claim` JWT hook, all 14 tables synced (`ingredient_measure` joined in 0009), `AnsiConnector` drains the queue soft-delete-safe **and jsonb-safe** (decodes `steps`/`eaters`/`macros`, clears `deleted_at` on PUT — both unit-tested against real `ps_crud`). Session is a sealed state machine (retry/sign-out, cached-household offline relaunch, queued auth events). Onboarding/hook/RLS pinned by ~50 pgTAP assertions. **Verified live**: local + cloud E2E (0008), on-device hardening pass (7.4), and the auth-aware `make test-sim` smoke now round-trips recipes and plan entries through live sync. Cloud health is now scripted read-only (`scripts/cloud_verify.sh` + `cloud.env` + committed `docker/powersync-cloud.streams.yaml` with a CI drift check; ledger in `docs/cloud-setup.md`). **Untested**: Google browser flow, offline-queue drain, two-client concurrent session (tracker). **2026-09-02:** `SyncStatus` and `getUploadQueueStats()` are finally read — `sync_health.dart` derives four states (settled · waiting · stalled past five minutes · refused) from a pure, unit-tested function, and the connector's fatal-drop path now reports a persisted `DroppedWrite` beside its `debugPrint` (asserted in `connector_test.dart`). The offline-queue drain is still untested, but for the first time there is a surface that would show it. **2026-09-03:** `ingredient.allowed_units` had been uploaded as a jsonb *string* since 0012 — the connector's jsonb map never listed it, so `allowed_units ? unit` was false server-side on every app-created row (pgTAP `unit_admission` #49 over smoke-created rows found it). Fixed in the map, repaired by `0028`, and the map is now **derived-checked** against the migrations by `test/structure/jsonb_columns_test.dart`. |
| Planning (`features/planning`) | 🟢 | Week grid + the Everyone/per-person lens (was Shared/Per-person until Week v2), two-step add flow — **recipe picker v2** (7.7: Recent · Books · Favorites tabs, day-tagged already-this-week chips, honest rows with recency + per-serving macros/incomplete badge, eating footer) → **confirm v2** (combined Day · Slot dropdown, macro line on the picked card, the full batch prose); slot grouping, copy-last-week; repo + domain + widget tests (incl. picker tabs + `watchLastPlanned`); verified on the sim. **Week v2 (polish pass, 2026-09-02):** one screen with Edit/Done, the **viewed week** as app state that Cook and Shop follow (`0019` scopes check-offs to it), the switcher's chevrons and menu, cook markers under each dish read back from `buildCookPlan`, honest day/week macros (`sumPlannedMacros`: sum what resolved, state the denominator, name every exclusion, never `0 kcal`), the empty week as a state of the screen, the entry sheet retiring the dish menu and the eaters dialog; the lens dims rather than filters. The sim's week flow (now `week_test.dart`) rewritten; it found and pinned the entry sheet's double pop under the shell. **0025 #7:** the switcher is the title of all three week tabs (one viewed week, D7a); the Week's "Cook and Shop follow it too" banner and both return pills are retired, its menu rows carry the tab's own count (`9 meals`), and `WeekSwitcher(showCopyLastWeek:)` keeps the Week write off the derived tabs (`week_header_test.dart`). **A usual portion per person (plan 0027 front P, 2026-09-03):** `household_member.portion_factor` (`0026`, default 1, check ¼–3, the table's first client UPDATE door — household-scoped policy, column-narrow grant) is set from a **Household** sheet off the Library `⋯` (×½ · ×¾ · ×1 · ×1¼ · ×1½ · custom in quarter steps; either member sets either's), and demand = Σ of the eaters' factors (`demandPortions`), the whole-number override still winning. Fractional everywhere and **printed as a fraction** (`core/units/portions.dart`: ¼ ½ ¾ glyphs, never 1.75): the Portions row ("1¾ portions — Ada 1 · Jun ¾ — their usual"; an override's small print "overrides the eaters' 1¾"), the per-person lens weighting by the factor (× override ÷ Σ factors) and naming its denominator ("1 meal · Jun · ¾ of 1¾ portions"). Pure-domain tests incl. the **identity test** (every existing fixture shape through the new derivations with all factors 1 — demand, macros under every lens, the cook plan, the printed strings — byte-identical); real-db repo tests (set + watch, the derived tabs re-deriving on a factor change); widget tests over the sheet, both Portions rows and the lens line; pgTAP `portion_factor.sql`. **Not yet driven on the sim** (the week file's P-D3/D4 leg is the orchestrator's at landing). |
| Cook-plan (`features/cook_plan`) | 🟢 | Derived Cook screen: greedy shelf-life clustering + freezer merge, session cards with timeline/notes; **whole-batch nudge** on fractional sessions (step 7.6: `wholeBatchNudgeFor`, display-level toggle — the raw factor stays what everything scales by); pure-Dart domain + format fully tested, repo tested on real PowerSync views, screen widget-tested; verified on the sim. Interactive cook-day still deferred (stretch). **8.6:** component lines derive real **batch-denominated** sessions ("X · for parent", never merged with portion sessions), depth-first with a visited-set cycle guard; unresolved yield renders the first-class gap card quoting the demanding line — never ×1 (frame f, verified on-sim). **0025 #7:** the header is the week switcher alone — no "Batch cook plan", no pill, no copy-last-week — and its menu says `2 cooks` / `nothing to cook` for the week it has derived (`cook_screen_test`); the smoke anchors on `CookView.rootKey`. **Plan 0027 front P:** `CoveredMeal.portions` / `CookSession.totalPortions` are doubles — a meal's demand is Σ of its eaters' portion factors (read through the same `eatersDemand` + `loadMembers` the Week and the shopping repo use, and `household_member` joins the watch so a factor change re-derives), batches = demand ÷ `servings_base` exactly as before, and the session row and the whole-batch nudge speak the fraction ("covers Mon dinner · 1¾ portions", "2¼ portions left over"). With every factor at 1 nothing changes — the identity test in `test/features/planning/` says so for the plan, the nudge and the words. |
| Shopping (`features/shopping`) | 🟢 | Derived Shop screen: aisle-grouped list summed from the cook plan with provenance + check-off; honest `aggregateQuantities` (folds measure-quantified lines into the **basis** subtotal — mass for per-g, volume for per-ml, 7.8) + `buildShoppingList` fully unit-tested; **whole-unit hint** ("2.25 → buy 3" beside the total, never replacing it — denominated in the measure the contributions actually used, un-gated for plain counts and measure-bearing mass items); bad data degrades visibly (invalid-measure / unrecognised-unit breakdown notes, never silent drops or unscaled sums); an unresolved `measure_id` survives the edit-top-up round-trip (widget + repo regression tests); thin overlay (`0006` + `0009` measure FK); repo tested on real PowerSync views (incl. recipe-deletion lifecycle, two-device duplicate-entry merge, measure degradation); verified on the sim. Package-size flags stay step 11. **8.6:** component sessions contribute their target's lines scaled by batch factor with **two-level provenance** ("Romesco Aioli · for Sliders"); the component line itself never becomes an item; an unresolved component contributes nothing and the parent's header says so (verified on-sim). **0025 #7:** the header is the week switcher alone — no "Shopping list", no pill, no copy-last-week — with the D9 status line kept under it; its menu says `6 items` / `nothing to buy` for the week it has summed; the smoke anchors on `ShoppingView.rootKey`. **Plan 0025 #6:** an optional line never becomes an item — dropped at the `effectiveLines` seam in the derivation, where lines meet the week — and the recipe's muted echo row says `N optional lines not listed — …` (domain, real-db repo and screen tests; not yet driven on the sim). |
| Import pipeline, server (`supabase/functions`) | 🟢 | Step 8 shipped and runs live. One forced LLM path: intake (`jsonld.ts` — JSON-LD → page-text fallback, or a vision transcription) → **① sanitize** via `ExtractAdapter` (**Claude Haiku 4.5**, picked by lane D's benchmark) → §7 `normalize` → the deterministic **cascade** (`match.ts`, exact → pg_trgm, bands 0.85/0.55, no embedding tier, **no silent auto-stub**) over the household vocab, household scoped from the JWT claim and never the request body (ADR-0004 intact; `usda_food` never a match target, ADR-0005). Tested without network or keys: JSON-LD against saved fixtures, normalize + unit-hints units, `match.test.ts` (band boundaries, exact-vs-trigram precedence, alias hits, calibration over the real vocab), `match_db.test.ts`, spine wiring on fakes, a **TS→Dart golden payload contract** test sharing one fixture with the app, and a **real-cascade e2e** running a blessed gold recipe over `vocab.jsonl`. The stub-enrichment leg is no longer TS: step 8.5 ported `prefillStubFromUsda` to a plpgsql trigger (`0014`/`0015`) and **deleted the callerless original** — a contract with no production caller is deleted, not left to drift. **Gaps:** the provider adapters' live behaviour is exercised only by `evals/`, not by CI. **8.6:** matching additionally offers household recipe **titles** (`recipe_candidates`, additive + omitted-when-empty — golden payload byte-identical; exact-or-trigram, stricter than the ingredient cascade, never auto-links); extraction/prompts/gold deliberately untouched. |
| Import client (`features/import`) | 🟢 | Intake (URL · photos from the system picker, each cropped/rotated then downscaled off the UI isolate) → the merged single editable **Review recipe** screen (cards compact-by-default expanding in place, band-based auto-lock, ingredient + unit "did you mean" chips, the 7.7 quantity/unit sheet reused for amounts, source line always visible, never-invent flags surfaced, Save gated on all-valid) → commit. Pure-Dart domain (`line_resolution`, `line_validation`, `preview_recipe`) unit-tested; repo tested on real PowerSync views incl. the `line_index` → `line_item_id` remap, `measure_id` resolution, and filing into the default book; payload parsing pinned against the server's golden fixture. **Create-new at review is the app's one add flow since 2026-09-03 (plan 0025 #3):** the card's "create" opens the New-ingredient sheet with the line's text, pushes the flesh-out form over it, and resolves the line to the row on back — the commit-time `import_stub` leg (`CommitStub`, `stubKey`, `createStubName`, the coalescing) is retired, and the repository test pins that a commit creates no ingredient. Verified live on the sim (URL + a gallery cookbook photo). Scenario 4 (import → reconcile → commit) runs green on the sim, tail included, and step 8.5's field test hardened the review further: the per-ingredient measure **stream** providers were silently completing into `const []` when an element disposed before PowerSync's first emission — so every measure word validated against nothing — now one batched `measuresByIngredients` query for the whole import, pinned by a real-schema provider test (plan 0020 J2). **Gaps:** there is no in-app **camera capture** — `pickMultiImage()` is the gallery picker, so "photograph the page" is a two-step (shoot in Camera, then pick), and nothing has run on a physical phone; a handful of review-screen polish items — all tracked; the 8.6 review leg (the "↪ your recipe" offer chip, linked-line Save arithmetic + `buildCommit` seam assertion, the MAKES row over `yield_raw` with its deliberately strict prefill parse) is host-tested only — the sim's canned payload carries no candidates (tracker). **Round three (plan 0025 #4, 2026-09-03):** the review's own `_ServingsRow` / `_MakesRow` are gone — it renders the editor's `RecipeHeaderForm` over a header draft `Recipe` (`headerDraft`: title, servings, a plain-yield prefill, the printed times; shelf life unset, filed into the default book), the controller implements `RecipeHeaderHost`, "not printed — set it" and "from source: …" ride the form's note slot, and `buildCommit` + the INSERT write every header column the editor writes (title · yield×2 · times · shelf life · book · section — real-db test, plus a structural test pinning the two INSERT column lists equal). **Post-await writes (2026-09-03, owner report #1):** the review is a viewport, and on a phone the search sheet's keyboard shrinks it — the card that opened the sheet can unmount before the pick comes back, and Riverpod 3 throws on a `WidgetRef` used after unmount, so the swap (and the amount sheets' writes) were lost. The three post-await writes now go through the app-lifetime container captured at build; a widget test unmounts the card under the open sheet and asserts the pick lands. The other five await-a-sheet sites bail on `context.mounted` instead (tracker). |
| Evals (`evals/`) | 🟡 | Real and useful, deliberately not a gate. Extraction: 11 human-blessed gold labels (the oracle, never mutated), programmatic scorers (line P/R after fuzzy align, qty/unit, servings, normalize-agreement, JSON-valid rate, the **hallucination/force-fit ledger**, confidence calibration) + an LLM-judge for prose only, reported per-stage and per-path; this is what chose the provider (98.9% vs 93.4% line-F1). Matching: `cases.jsonl` generated from the live `vocab.jsonl`. **Why 🟡 and not 🟢:** the **matching** eval now runs in CI (`evals.yml` — dispatch plus a Monday cron, reporting drift rather than gating), but the **extraction** half still needs real provider keys and a local photo corpus, so the half that chose the provider is unprotected from bit-rot; the scorer and gold need a human in the loop (the residual gold ambiguities are a tracker row); and run output isn't committed, so there's no trend line yet. |
| Errors & sync health (cross-cutting) | 🟢 | Front 6 of the polish pass, 2026-09-02. The audit found 83 error paths, 11 honest. Now: **one door** for every UI→repository write (`shared/write.dart`) held by a structural test that derives its write set from the `domain/` interfaces, so a new method is covered the day it is declared — 43 call sites swept across seven features, including the 8 fire-and-forget ones and the recipe editor's Save, where a throw used to show only as the editor not navigating. **One error state** with a mapped reason and a retry replaces six reasonless shrugs, and the four load-bearing `?? const []` sites became real error states (an errored `usedIn` could turn a delete refusal into a delete; an errored measures stream silently narrowed admissible units). **One sync-health provider** feeds three readouts that cannot disagree — the shell's banner, the Library `⋯` line, the Shop list's line in its own noun — with the copy pinned by a golden-text test that also enforces that the word *offline* never appears. The zone handler's `TODO(observability)` is gone, replaced by a `CrashSink` seam with a rate-limited, coalescing toast and no vendor. Doc: [`design-docs/errors-and-sync-health.md`](./design-docs/errors-and-sync-health.md). **Gaps:** the structural check is a regex (limits stated in its own doc comment, tracker row), and the stall escalation has never run against a genuinely failing stack (tracker). **Post-await writes (2026-09-03):** a write after an awaited sheet, dialog or prompt goes through `container.write(host, …)` with the container and `hostContextOf` captured before the await — never the widget's `ref` (Riverpod 3 throws once the row has unmounted under the keyboard) and never a `context.mounted` bail (which dropped the confirmed action). Held by `test/structure/no_ref_after_await_test.dart`. |
| Docs / harness | 🟢 | Structure in place; `make docs-check` enforces integrity; `make docs` really generates `docs/generated/db-schema.md`; step-done checklist in the exec-plan template. |
| CI | 🟢 | **Really running, on GitHub, since 2026-09-01** — `github.com/SimonBoothroyd/ansi`, six workflows: `app` (format · analyze · custom_lint · `flutter test` over a real `PowerSyncDatabase` on `ubuntu-latest` · the domain-purity grep), `backend` (deno fmt/lint/test for edge functions *and* seed scripts, plus a migrations job that reproduces `make db-reset` + `supabase test db`), `docs` (`check_docs.sh` — links, required files, stream drift), `evals` (dispatch + a Monday cron, calibration not a gate), `release` (tag-triggered, [release.md](./release.md)) and `deploy-supabase` (dispatch-only). All four push-triggered workflows were green on `main` at the time of writing. **Two pins are load-bearing and were bought with red runs:** Flutter is `3.47.1` *exactly* in both `app.yml` and `release.yml` (3.47.2's Dart adds an analyze warning), and `config.toml`'s `major_version = 17` matches the hosted project — CI never links, so it fell back to that line and was silently testing on Postgres 15 while every dev machine ran 17 (the file's own comment records the `.temp/postgres-version` precedence trap). The sim smoke (`make test-sim`) stays a local gate by choice — it needs a booted simulator. Since plan 0026 (2026-09-03) it is one file per flow under `app/integration_test/` (`auth` · `recipe_editor` · `week` · `import` · `ingredients` · `nested`) over a `support/` library: each file provisions its own household over HTTP and signs in programmatically (only `auth_test.dart` drives the gate), and seeds its prerequisites through the app's own repositories over the throwaway db; `make test-sim FILE=week` runs one file, `DEVICE=<udid>` picks the simulator. |

## The bar for "done" on any slice

- Logic lives in the right layer (domain is pure Dart).
- Tests cover the logic, and they'd fail if the logic broke.
- Behaviour-affecting changes updated the relevant doc.
- `make ci` and `make docs-check` are green.
