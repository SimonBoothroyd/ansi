See [AGENTS.md](./AGENTS.md).

This project uses AGENTS.md as the single agent entry point (a *map*, per
OpenAI's harness-engineering model — see `docs/README.md`). This pointer exists
so Claude Code discovers it too. The real sources of truth live under `docs/`.
